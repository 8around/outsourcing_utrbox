# 002_데이터베이스_스키마_분석

## 📌 문서 정보
- **작성일**: 2025-10-23
- **분석 대상**: DATABASE_SCHEMA.md
- **비교 기준**: utrbox_prd_final.md
- **목적**: 데이터베이스 스키마의 오버스펙 부분 식별 및 최적화 방안 제시

---

## 📋 목차
1. [분석 개요](#분석-개요)
2. [명확한 오버스펙 항목](#명확한-오버스펙-항목)
3. [불명확 항목 (확인 필요)](#불명확-항목-확인-필요)
4. [권장 조치 사항](#권장-조치-사항)
5. [다음 단계](#다음-단계)

---

## 분석 개요

DATABASE_SCHEMA.md에 정의된 Supabase 데이터베이스 스키마를 PRD 요구사항과 비교하여 불필요하거나 과도하게 설계된 부분을 식별했습니다.

### 분석 방법
- PRD의 명시적 요구사항과 스키마의 각 컬럼/테이블 비교
- 기능적 필요성 vs 확장성 목적 구분
- 실무적 유용성 vs 오버엔지니어링 판단

---

## 명확한 오버스펙 항목

### 🔴 1. activity_logs 테이블 전체

**현재 구조**:
```sql
CREATE TABLE public.activity_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES public.users(id) ON DELETE SET NULL,
  action TEXT NOT NULL,
  entity_type TEXT,
  entity_id UUID,
  ip_address INET,
  user_agent TEXT,
  metadata JSONB DEFAULT '{}'::jsonb,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**문제점**:
- PRD에 활동 로그, 감사 로그 요구사항이 전혀 없음
- 관련 트리거 `log_user_login`도 PRD 범위 밖
- IP 주소, User Agent 수집 등 개인정보 처리 요구사항 없음

**권장 조치**:
- ✅ **테이블 전체 삭제**
- ✅ **log_user_login 트리거 삭제**
- ✅ **관련 인덱스 전체 삭제**

---

### 🔴 2. 모든 metadata JSONB 컬럼

**영향받는 테이블**:
- `users.metadata`
- `collections.metadata`
- `contents.metadata`
- `detected_contents.metadata`

**문제점**:
- 확장성을 위한 "만약을 위한" 설계
- PRD에 추가 메타데이터 저장 요구사항 없음
- JSONB는 인덱싱과 쿼리 복잡도 증가

**권장 조치**:
- ✅ **모든 metadata 컬럼 삭제**
- 향후 필요 시 마이그레이션으로 추가

---

### 🔴 3. collections 테이블 - 불필요 컬럼

#### 3.1 description
```sql
description TEXT,
```

**문제점**:
- PRD에 "컬렉션 생성, 이름 수정, 삭제"만 언급
- 컬렉션 설명 필드 요구사항 없음

**권장 조치**: ✅ **삭제**

#### 3.2 total_size
```sql
total_size BIGINT DEFAULT 0,
```

**문제점**:
- 저장 용량 제한, 통계 표시 요구사항 없음
- 트리거로 자동 관리되지만 사용처 없음

**권장 조치**: ✅ **삭제** (관련 트리거 로직도 제거)

---

### 🔴 4. contents 테이블 - 불필요 컬럼

#### 4.1 width, height
```sql
width INTEGER,
height INTEGER,
```

**문제점**:
- 이미지 크기 정보 표시 요구사항 없음
- Google Vision API가 제공할 수 있지만 PRD에서 사용 언급 없음

**권장 조치**: ✅ **삭제**

---

### 🔴 5. analysis_results 테이블 - 불필요 컬럼

#### 5.1 processing_time_ms
```sql
processing_time_ms INTEGER,
```

**문제점**:
- 성능 모니터링, 통계 요구사항 없음
- API 응답 시간 추적 필요성 PRD에 없음

**권장 조치**: ✅ **삭제**

#### 5.2 api_version
```sql
api_version TEXT,
```

**문제점**:
- API 버전 관리 요구사항 없음
- Google Vision API 버전 추적 필요성 PRD에 없음

**권장 조치**: ✅ **삭제**

#### 5.3 processed_data
```sql
processed_data JSONB,
```

**문제점**:
- `raw_response`에 이미 전체 API 응답 저장
- 가공된 데이터를 별도 저장하는 이유 불명확
- 데이터 중복 가능성

**권장 조치**: ✅ **삭제** (raw_response만 사용)

---

## 불명확 항목 (확인 필요)

### 🟡 1. users.email_verified

**현재 구조**:
```sql
email_verified BOOLEAN DEFAULT FALSE,
```

**PRD 언급**:
> "이메일 인증 기반 비밀번호 찾기"

**문제점**:
- "이메일 인증"의 의미가 불명확
  - 회원가입 시 이메일 인증? (이메일로 링크 발송 → 클릭 → 인증 완료)
  - 비밀번호 찾기 시 이메일로 링크 발송? (인증 없이 단순 링크 전송)

**질문**:
1. 회원가입 시 이메일 인증이 필요한가요?
2. 이메일 미인증 사용자는 로그인이 불가능한가요?

**권장 조치**:
- ✅ **이메일 인증 필요**: 컬럼 유지
- ❌ **단순 링크 발송**: 컬럼 삭제

---

### 🟡 2. users.last_login_at

**현재 구조**:
```sql
last_login_at TIMESTAMP WITH TIME ZONE,
```

**장점**:
- 회원 관리 화면에서 "마지막 로그인" 표시 가능
- 휴면 계정 관리에 유용

**단점**:
- PRD의 "회원 관리" 표시 항목에 "마지막 로그인" 없음
- activity_logs 삭제 시 의미 있지만, 그것도 PRD 범위 밖

**질문**:
1. 관리자 회원 관리 화면에서 "마지막 로그인 시간"을 표시할 예정인가요?

**권장 조치**:
- ✅ **표시 예정**: 컬럼 유지
- ❌ **불필요**: 컬럼 삭제

---

### 🟡 3. collections.content_count

**현재 구조**:
```sql
content_count INTEGER DEFAULT 0,
```

**장점**:
- 컬렉션별 콘텐츠 수를 빠르게 표시 가능
- 트리거로 자동 관리되어 일관성 보장
- 일반적인 UX 패턴 (폴더 옆에 파일 개수 표시)

**단점**:
- PRD에 명시 없음
- COUNT 쿼리로 대체 가능 (성능은 약간 느림)

**질문**:
1. 컬렉션 리스트에서 각 컬렉션의 콘텐츠 수를 표시할 예정인가요?

**권장 조치**:
- ✅ **UX 개선 목적**: 컬럼 유지 (권장)
- ❌ **불필요**: 컬럼 삭제 + 트리거 로직 제거

---

### 🟡 4. contents.match_count

**현재 구조**:
```sql
match_count INTEGER DEFAULT 0,
```

**장점**:
- 관리자가 "일치" 판정한 발견 콘텐츠 수 통계
- detection_count (전체 감지 수)와 match_count (일치 판정 수) 구분

**단점**:
- PRD에는 "감지 건수"만 언급
- "일치 건수"는 별도 언급 없음

**질문**:
1. 원본 콘텐츠 리스트에서 "감지 건수"와 "일치 건수"를 모두 표시할 예정인가요?
2. 아니면 "감지 건수"만 표시하나요?

**권장 조치**:
- ✅ **둘 다 표시**: 컬럼 유지
- ❌ **감지 건수만**: 컬럼 삭제 + 트리거 로직 제거

---

### 🟡 5. contents.error_message

**현재 구조**:
```sql
error_message TEXT,
```

**장점**:
- AI 분석 실패 시 원인 파악 가능
- 디버깅 및 에러 트러블슈팅에 유용

**단점**:
- PRD에 에러 메시지 표시 요구사항 없음
- 사용자/관리자에게 에러 내용을 보여줄지 불명확

**질문**:
1. AI 분석 실패 시 에러 메시지를 사용자/관리자에게 표시할 예정인가요?

**권장 조치**:
- ✅ **디버깅 목적으로라도 유지 권장** (실무적 유용성)
- ❌ **불필요**: 삭제

---

### 🟡 6. contents.analyzed_at

**현재 구조**:
```sql
analyzed_at TIMESTAMP WITH TIME ZONE,
```

**장점**:
- 분석 완료 시간 추적
- 분석 이력 관리 가능

**단점**:
- PRD에 명시 없음
- created_at, updated_at으로 대략적 시간 파악 가능

**질문**:
1. "분석 완료 시간"을 별도로 추적할 필요가 있나요?

**권장 조치**:
- ✅ **이력 관리 목적**: 컬럼 유지
- ❌ **불필요**: 컬럼 삭제

---

### 🟡 7. detected_contents.similarity_score

**현재 구조**:
```sql
similarity_score DECIMAL(5, 4) CHECK (similarity_score >= 0 AND similarity_score <= 1),
```

**장점**:
- 유사도 점수 기반 정렬/필터링 가능
- 높은 유사도 우선 검토 등 워크플로우 개선

**단점**:
- PRD에 유사도 점수 표시 요구사항 없음
- Google Vision API가 유사도 점수를 제공하는지 확인 필요

**질문**:
1. Google Vision API 응답에 유사도 점수가 포함되나요?
2. 발견 콘텐츠 리스트에서 유사도 점수를 표시할 예정인가요?

**권장 조치**:
- ✅ **API 제공 + 표시 예정**: 컬럼 유지
- ❌ **불필요**: 컬럼 삭제

---

### 🟡 8. detected_contents.review_note

**현재 구조**:
```sql
review_note TEXT,
```

**장점**:
- 관리자가 검토 시 메모 작성 가능
- 판정 이유 기록으로 커뮤니케이션 개선

**단점**:
- PRD에 메모/코멘트 기능 없음
- 관리자 워크플로우에 추가 입력 부담

**질문**:
1. 관리자가 "일치/불일치/비교불가" 판정 시 메모를 작성하도록 할 예정인가요?

**권장 조치**:
- ✅ **메모 기능 필요**: 컬럼 유지
- ❌ **불필요**: 컬럼 삭제

---

## 권장 조치 사항

### ✅ 즉시 제거 가능 (오버스펙 확실)

| 항목 | 삭제 대상 | 관련 작업 |
|------|-----------|-----------|
| 테이블 | `activity_logs` | 테이블 전체 DROP |
| 트리거 | `log_user_login` | 트리거 DROP |
| 인덱스 | `idx_activity_logs_*` | 인덱스 전체 DROP |
| 컬럼 | 모든 `metadata JSONB` | 6개 테이블에서 컬럼 DROP |
| 컬럼 | `collections.description` | ALTER TABLE DROP COLUMN |
| 컬럼 | `collections.total_size` | ALTER TABLE DROP COLUMN + 트리거 수정 |
| 컬럼 | `contents.width` | ALTER TABLE DROP COLUMN |
| 컬럼 | `contents.height` | ALTER TABLE DROP COLUMN |
| 컬럼 | `analysis_results.processing_time_ms` | ALTER TABLE DROP COLUMN |
| 컬럼 | `analysis_results.api_version` | ALTER TABLE DROP COLUMN |
| 컬럼 | `analysis_results.processed_data` | ALTER TABLE DROP COLUMN |

### 🟡 개발자 확인 필요

다음 항목들은 PRD에 명시되지 않았지만 실무적 유용성이 있거나 요구사항 해석이 필요합니다:

1. **이메일 인증**: `users.email_verified` 필요 여부
2. **마지막 로그인**: `users.last_login_at` 표시 계획
3. **컬렉션 통계**: `collections.content_count` 표시 계획
4. **일치 건수**: `contents.match_count` 표시 계획
5. **에러 메시지**: `contents.error_message` 사용 계획
6. **분석 시간**: `contents.analyzed_at` 필요 여부
7. **유사도 점수**: `detected_contents.similarity_score` 제공 여부 및 표시 계획
8. **검토 메모**: `detected_contents.review_note` 사용 계획

### 💚 유지 권장 (실무적 유용성)

개발자 확인 결과와 관계없이 다음은 유지를 권장합니다:

1. **contents.error_message** - 디버깅 목적
2. **collections.content_count** - 일반적인 UX 패턴
3. **contents.match_count** - 통계 및 필터링 유용

---

## 다음 단계

### 1단계: 개발자 확인
위 "개발자 확인 필요" 섹션의 8개 질문에 답변해주세요.

### 2단계: 최종 스키마 작성
확인 결과를 반영하여 최종 최적화된 스키마를 작성합니다.

### 3단계: 마이그레이션 계획
기존 DATABASE_SCHEMA.md를 수정하고, Supabase 마이그레이션 스크립트를 작성합니다.

### 4단계: 적용
- DATABASE_SCHEMA.md 업데이트
- Supabase 마이그레이션 실행
- 관련 TypeScript 타입 정의 업데이트

---

## 참고: 삭제 예상 효과

### 테이블 수 감소
- 현재: 6개 테이블
- 삭제 후: 5개 테이블 (-16.7%)

### 컬럼 수 감소
- users: 12개 → 9~11개 (-1~3개)
- collections: 9개 → 5~6개 (-3~4개)
- contents: 17개 → 11~14개 (-3~6개)
- analysis_results: 7개 → 4개 (-3개)
- detected_contents: 11개 → 9~10개 (-1~2개)

### 트리거/함수 수 감소
- 현재: 5개 트리거, 4개 함수
- 삭제 후: 4개 트리거, 3개 함수

### 인덱스 수 감소
- activity_logs 관련 인덱스 4개 삭제
- metadata GIN 인덱스 2개 삭제
- 기타 컬럼 삭제로 인한 인덱스 정리

---

**작성자**: Claude Code (AI Assistant)
**검토 필요**: 개발자 확인 후 최종 결정
