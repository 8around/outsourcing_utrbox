# 029. 회원 상세 페이지 실제 데이터 연동 및 UI 개선

## 📋 개요

관리자 회원 상세 페이지(`app/admin/users/[id]/page.tsx`)를 Mock 데이터에서 Supabase 실제 데이터로 전환하고, 회원 정보 편집 및 콘텐츠 관리 UI를 개선합니다.

## 🎯 목표

1. **실제 데이터 조회**: Mock 데이터 → Supabase 데이터베이스 연동
2. **회원 정보 편집**: 이름 및 소속 변경 기능 추가
3. **계정 관리 개선**: 자기 자신에 대한 차단/권한 변경 제한
4. **콘텐츠 UI 개선**: 툴바 기반 검색/정렬 및 테이블 뷰 구현

## 📐 현재 상태 분석

### 기존 구현
- **데이터 소스**: `lib/admin/mock-data.ts` (Mock 데이터)
- **회원 정보**: 읽기 전용 카드 형식
- **계정 관리**: 승인/차단/권한 변경 (제한 없음)
- **콘텐츠 목록**: 단순 테이블 형식

### 기존 API 함수 (활용 가능)
- `lib/api/users.ts`:
  - `getUser(id)`: 사용자 조회 ✅
  - `updateUserApproval(id, isApproved)`: 승인 상태 변경 ✅
  - `updateUserRole(id, role)`: 역할 변경 ✅
- `lib/api/contents.ts`:
  - `getContentsWithPagination()`: 관리자용 전체 콘텐츠 조회 ✅

### 부족한 부분
- `updateUserInfo(id, data)`: 이름/소속 변경 API 없음 ❌
- `getUserContentsWithPagination(userId, filters)`: 특정 사용자 콘텐츠 조회 API 없음 ❌
- 현재 로그인한 관리자 정보 조회 기능 없음 ❌
- 회원 정보 편집 UI 없음 ❌
- 콘텐츠 필터링 툴바 없음 ❌

## 🗂️ 작업 계획

### Phase 1: API 계층 구현

#### Task 1.1: 회원 정보 수정 API 추가
**파일**: `lib/api/users.ts`

**작업 내용**:
```typescript
/**
 * 사용자 정보를 수정합니다.
 * @param id - 사용자 ID
 * @param data - 수정할 정보 (name, organization)
 * @returns ApiResponse<null> - 성공 또는 에러
 */
export async function updateUserInfo(
  id: string,
  data: { name?: string; organization?: string }
): Promise<ApiResponse<null>>
```

**구현 세부사항**:
- Supabase `users` 테이블 UPDATE
- `updated_at` 자동 갱신
- 에러 처리 및 로깅

**의존성**: 없음

---

#### Task 1.2: 사용자 콘텐츠 조회 API 추가
**파일**: `lib/api/contents.ts`

**작업 내용**:
```typescript
/**
 * 특정 사용자의 콘텐츠를 페이지네이션과 필터링을 적용하여 조회합니다.
 * @param userId - 사용자 ID
 * @param params - 조회 파라미터 (page, pageSize, is_analyzed, search, sortBy, sortOrder)
 * @returns PaginatedApiResponse<Content[]> - 콘텐츠 목록 및 전체 개수 또는 에러
 */
export async function getUserContentsWithPagination(
  userId: string,
  params: {
    page?: number
    pageSize?: number
    is_analyzed?: boolean | null
    search?: string
    sortBy?: 'name' | 'date'
    sortOrder?: 'asc' | 'desc'
  }
): Promise<PaginatedApiResponse<Content[]>>
```

**구현 세부사항**:
- 기존 `getContentsWithPagination` 참고
- `user_id` 필터 추가
- `users` JOIN 제거 (업로더 정보 불필요)
- `collections` JOIN 유지
- `detected_count` 계산 포함

**참고**: `getContentsWithPagination` (line 413-507)

**의존성**: 없음

---

### Phase 2: 회원 정보 편집 UI

#### Task 2.1: UserDetailCard 편집 모드 추가
**파일**: `components/admin/users/UserDetailCard.tsx`

**작업 내용**:
1. **상태 관리**:
   - `isEditing`: 편집 모드 여부
   - `editData`: 편집 중인 데이터 ({name, organization})

2. **UI 변경**:
   - 읽기 모드: 기존 표시 + "편집" 버튼
   - 편집 모드: Input 필드 + "저장/취소" 버튼

3. **Props 추가**:
   ```typescript
   interface UserDetailCardProps {
     user: User
     onUpdate: (data: { name?: string; organization?: string }) => Promise<void>
   }
   ```

4. **검증**:
   - 이름 필수 입력
   - 소속 선택사항

**의존성**: Task 1.1 (API 함수)

---

### Phase 3: 계정 관리 개선

#### Task 3.1: 현재 사용자 정보 조회
**파일**: `app/admin/users/[id]/page.tsx`

**작업 내용**:
1. **Supabase Auth 사용**:
   ```typescript
   import { createClient } from '@/lib/supabase/client'

   const supabase = createClient()
   const { data: { user: currentUser } } = await supabase.auth.getUser()
   const currentUserId = currentUser?.id
   ```

2. **현재 사용자 ID 전달**:
   - `UserActionButtons` 컴포넌트에 props 추가

**의존성**: 없음

---

#### Task 3.2: UserActionButtons 자기 자신 제한
**파일**: `components/admin/users/UserActionButtons.tsx`

**작업 내용**:
1. **Props 추가**:
   ```typescript
   interface UserActionButtonsProps {
     user: User
     currentUserId?: string
     onApprove: () => void
     onBlock: () => void
     onRoleChange: (role: 'member' | 'admin') => void
   }
   ```

2. **제한 로직**:
   ```typescript
   const isSelf = user.id === currentUserId

   // 차단 버튼
   disabled={user.is_approved === false || isSelf}

   // 권한 변경 버튼
   disabled={isSelf}
   ```

3. **툴팁 추가**:
   - 자기 자신인 경우: "자신의 계정은 수정할 수 없습니다"

**의존성**: Task 3.1

---

### Phase 4: 콘텐츠 섹션 UI 구현

#### Task 4.1: UserContentToolbar 컴포넌트 생성
**파일**: `components/admin/users/UserContentToolbar.tsx` (신규)

**작업 내용**:
1. **참고 컴포넌트**: `components/explorer/ExplorerToolbar.tsx`

2. **포함 요소**:
   - ✅ 검색 입력 필드
   - ✅ 정렬 선택 (업로드순/이름순)
   - ✅ 정렬 방향 토글 (오름차순/내림차순)
   - ❌ 뷰모드 변경 (제외)
   - ❌ 컬렉션 선택 (제외)
   - ❌ 업로드 버튼 (제외)
   - ❌ 새로고침 버튼 (제외)

3. **Props**:
   ```typescript
   interface UserContentToolbarProps {
     searchQuery: string
     sortBy: 'name' | 'date'
     sortOrder: 'asc' | 'desc'
     onSearchChange: (query: string) => void
     onSortByChange: (sortBy: 'name' | 'date') => void
     onSortOrderToggle: () => void
   }
   ```

4. **스타일**:
   - ExplorerToolbar와 동일한 디자인 시스템 사용
   - 반응형 레이아웃

**의존성**: 없음

---

#### Task 4.2: UserContentTable 컴포넌트 생성
**파일**: `components/admin/users/UserContentTable.tsx` (신규)

**작업 내용**:
1. **참고 컴포넌트**: `components/admin/contents/ContentTableClient.tsx`

2. **컬럼 구성**:
   - ✅ 이미지 (썸네일)
   - ✅ 파일명 (클릭 시 상세 페이지)
   - ✅ 분석 상태 (Badge)
   - ✅ 발견 건수 (Badge)
   - ✅ 업로드일
   - ❌ 업로더 (제외)

3. **Props**:
   ```typescript
   interface UserContentTableProps {
     contents: Content[]
     totalCount: number
     currentPage: number
     pageSize: number
     loading: boolean
     onPageChange: (page: number) => void
   }
   ```

4. **기능**:
   - 로딩 중 스켈레톤 UI (ContentTableClient 참고)
   - 페이지네이션 (Pagination 컴포넌트 재사용)
   - 빈 상태 메시지

**참고**: ContentTableClient (line 35-277)

**의존성**: 없음

---

#### Task 4.3: 콘텐츠 카드 통합
**파일**: `app/admin/users/[id]/page.tsx`

**작업 내용**:
1. **기존 카드 교체**:
   - 기존 `업로드한 콘텐츠` Card → 제거
   - `UserContentToolbar` + `UserContentTable` 추가

2. **상태 관리**:
   ```typescript
   const [contentFilters, setContentFilters] = useState({
     page: 1,
     search: '',
     sortBy: 'date' as 'name' | 'date',
     sortOrder: 'desc' as 'asc' | 'desc',
   })
   const [contentsLoading, setContentsLoading] = useState(false)
   ```

3. **필터링 변경 시 로딩**:
   - 툴바 필터 변경 → `setContentsLoading(true)` → API 호출 → `setContentsLoading(false)`
   - 페이지 상단은 로딩하지 않고, 테이블만 스켈레톤 표시

**의존성**: Task 4.1, Task 4.2

---

### Phase 5: 메인 페이지 통합

#### Task 5.1: 실제 데이터 조회 구현
**파일**: `app/admin/users/[id]/page.tsx`

**작업 내용**:
1. **Mock 데이터 제거**:
   ```typescript
   // Before
   const user = mockUsers.find((u) => u.id === userId)
   const userContents = mockContents.filter((c) => c.user_id === userId)

   // After
   import { getUser } from '@/lib/api/users'
   import { getUserContentsWithPagination } from '@/lib/api/contents'
   ```

2. **데이터 페칭**:
   ```typescript
   const [user, setUser] = useState<User | null>(null)
   const [contents, setContents] = useState<Content[]>([])
   const [totalCount, setTotalCount] = useState(0)
   const [loading, setLoading] = useState(true)

   useEffect(() => {
     async function fetchUser() {
       const result = await getUser(userId)
       if (result.success) setUser(result.data)
       else toast({ title: '오류', description: result.error })
       setLoading(false)
     }
     fetchUser()
   }, [userId])

   useEffect(() => {
     async function fetchContents() {
       setContentsLoading(true)
       const result = await getUserContentsWithPagination(userId, contentFilters)
       if (result.success) {
         setContents(result.data || [])
         setTotalCount(result.totalCount)
       }
       setContentsLoading(false)
     }
     if (user) fetchContents()
   }, [userId, contentFilters])
   ```

**의존성**: Task 1.1, Task 1.2

---

#### Task 5.2: 이벤트 핸들러 구현
**파일**: `app/admin/users/[id]/page.tsx`

> **⚠️ 중요**: Phase 3 완료 시점에서 TypeScript 에러가 발생합니다:
> - `Property 'onUpdate' is missing in type '{ user: User; }' but required in type 'UserDetailCardProps'`
> - 이는 UserDetailCard의 onUpdate prop이 아직 전달되지 않았기 때문입니다.
> - **이 Task 5.2에서 handleUpdateUserInfo 함수를 구현하고 UserDetailCard에 onUpdate prop을 전달하면 해결됩니다.**

**작업 내용**:
```typescript
// 회원 정보 수정
const handleUpdateUserInfo = async (data: { name?: string; organization?: string }) => {
  const result = await updateUserInfo(userId, data)
  if (result.success) {
    toast({ title: '수정 완료', description: '회원 정보를 수정했습니다.' })
    // 사용자 정보 새로고침
    const userResult = await getUser(userId)
    if (userResult.success) setUser(userResult.data)
  } else {
    toast({ title: '오류', description: result.error, variant: 'destructive' })
  }
}

// 승인
const handleApprove = async () => {
  const result = await updateUserApproval(userId, true)
  if (result.success) {
    toast({ title: '승인 완료', description: '회원을 승인했습니다.' })
    const userResult = await getUser(userId)
    if (userResult.success) setUser(userResult.data)
  } else {
    toast({ title: '오류', description: result.error, variant: 'destructive' })
  }
}

// 차단
const handleBlock = async () => {
  const result = await updateUserApproval(userId, false)
  if (result.success) {
    toast({ title: '차단 완료', description: '회원을 차단했습니다.', variant: 'destructive' })
    const userResult = await getUser(userId)
    if (userResult.success) setUser(userResult.data)
  } else {
    toast({ title: '오류', description: result.error, variant: 'destructive' })
  }
}

// 권한 변경
const handleRoleChange = async (role: 'member' | 'admin') => {
  const result = await updateUserRole(userId, role)
  if (result.success) {
    toast({ title: '권한 변경 완료', description: `권한을 ${role === 'admin' ? '관리자' : '일반 회원'}으로 변경했습니다.` })
    const userResult = await getUser(userId)
    if (userResult.success) setUser(userResult.data)
  } else {
    toast({ title: '오류', description: result.error, variant: 'destructive' })
  }
}
```

**의존성**: Task 1.1, Task 1.2, Task 5.1

---

#### Task 5.3: 로딩 및 에러 처리
**파일**: `app/admin/users/[id]/page.tsx`

**작업 내용**:
1. **전체 페이지 로딩**:
   ```typescript
   if (loading) {
     return <div className="flex items-center justify-center h-96">
       <Loader2 className="h-8 w-8 animate-spin" />
     </div>
   }
   ```

2. **사용자 없음 처리** (기존 유지):
   ```typescript
   if (!user) {
     return <div>회원을 찾을 수 없습니다.</div>
   }
   ```

3. **콘텐츠 로딩**: Task 4.2의 스켈레톤 UI 활용

**의존성**: Task 5.1, Task 5.2

---

## 🔗 의존성 그래프

```
Phase 1 (병렬 가능)
├─ Task 1.1: updateUserInfo API
└─ Task 1.2: getUserContentsWithPagination API

Phase 2 (Task 1.1 필요)
└─ Task 2.1: UserDetailCard 편집 모드

Phase 3 (순차)
├─ Task 3.1: 현재 사용자 정보 조회
└─ Task 3.2: UserActionButtons 제한 (Task 3.1 필요)

Phase 4 (병렬 가능)
├─ Task 4.1: UserContentToolbar
├─ Task 4.2: UserContentTable
└─ Task 4.3: 콘텐츠 카드 통합 (4.1, 4.2 필요)

Phase 5 (Phase 1-4 완료 후)
├─ Task 5.1: 실제 데이터 조회 (1.1, 1.2 필요)
├─ Task 5.2: 이벤트 핸들러 (1.1, 1.2, 5.1 필요)
└─ Task 5.3: 로딩/에러 처리 (5.1, 5.2 필요)
```

## 📊 작업 순서 요약

**최적 실행 순서**:
1. Phase 1: Task 1.1, 1.2 병렬 실행
2. Phase 2: Task 2.1
3. Phase 3: Task 3.1 → 3.2 순차
4. Phase 4: Task 4.1, 4.2 병렬 → 4.3
5. Phase 5: Task 5.1 → 5.2 → 5.3 순차

## 🧪 테스트 계획

### 단위 테스트
- [ ] `updateUserInfo` API 함수 테스트
- [ ] `getUserContentsWithPagination` API 함수 테스트

### 통합 테스트
- [ ] 회원 정보 편집 플로우
- [ ] 계정 관리 플로우 (자기 자신 제한 포함)
- [ ] 콘텐츠 검색/정렬 플로우

### UI 테스트
- [ ] 편집 모드 전환
- [ ] 필터링 시 부분 로딩
- [ ] 에러 처리 Toast

## 🎨 UI/UX 개선 사항

1. **회원 정보 카드**:
   - 읽기 모드: 깔끔한 정보 표시
   - 편집 모드: 인라인 편집 (별도 페이지 이동 없음)

2. **계정 관리**:
   - 자기 자신인 경우 버튼 비활성화 + 툴팁 안내

3. **콘텐츠 목록**:
   - 툴바: 직관적인 검색/정렬 UI
   - 테이블: 일관된 디자인 (ContentTableClient와 동일)
   - 부분 로딩: 필터 변경 시 테이블만 로딩

## 📝 참고 사항

### 재사용 컴포넌트
- `components/ui/*`: shadcn/ui 기본 컴포넌트
- `components/explorer/Pagination`: 페이지네이션
- `hooks/use-toast`: Toast 알림

### 스타일 가이드
- Tailwind CSS 클래스 활용
- 기존 admin 페이지와 일관된 디자인 유지

### 성능 최적화
- 필터링 변경 시 debounce 적용 (검색 입력)
- 콘텐츠 이미지 lazy loading

## ✅ 완료 기준

- [ ] Mock 데이터 완전 제거
- [ ] 실제 Supabase 데이터 조회 정상 작동
- [ ] 회원 정보 편집 성공
- [ ] 자기 자신에 대한 계정 관리 제한 작동
- [ ] 콘텐츠 검색/정렬 정상 작동
- [ ] 부분 로딩 (필터링 시 테이블만) 정상 작동
- [ ] 에러 처리 및 Toast 알림 정상 작동
- [ ] 모든 테스트 통과

---

**작성일**: 2025-01-04
**관련 문서**:
- PRD.md
- DATABASE_SCHEMA.md
- docs/020_회원_관리_페이지_실제_데이터_연동_및_페이지네이션.md
- docs/024_콘텐츠_관리_페이지_실제_데이터_연동_및_개선.md
