# 014. 파일 탐색기 UI 전환 구현 계획

**작성일**: 2025-10-27
**태스크**: Task #7, #8
**목표**: 사이드바 네비게이션을 제거하고 파일 탐색기 스타일의 통합 UI로 전환

---

## 📋 목차

1. [개요](#1-개요)
2. [현재 상황 분석](#2-현재-상황-분석)
3. [목표 아키텍처](#3-목표-아키텍처)
4. [주요 변경사항](#4-주요-변경사항)
5. [컴포넌트 설계](#5-컴포넌트-설계)
6. [구현 단계](#6-구현-단계)
7. [데이터 흐름](#7-데이터-흐름)
8. [UI/UX 가이드라인](#8-uiux-가이드라인)
9. [테스트 계획](#9-테스트-계획)

---

## 1. 개요

### 1.1 배경
현재 사용자 페이지는 전통적인 사이드바 네비게이션 구조를 사용하고 있습니다:
- 대시보드, 콘텐츠, 컬렉션이 별도 페이지로 분리
- 각 페이지 간 이동이 필요
- 컬렉션과 콘텐츠의 계층적 관계가 명확하지 않음

### 1.2 목표
Windows 탐색기 또는 macOS Finder와 유사한 파일 탐색기 UI를 구현하여:
- 컬렉션(폴더)과 콘텐츠(파일)를 하나의 화면에서 관리
- 직관적인 계층 구조 표현
- 효율적인 네비게이션 및 작업 흐름

### 1.3 핵심 요구사항
- ✅ 사이드바 제거
- ✅ 대시보드 상단 통계 현황 유지 (총 콘텐츠, 분석 완료, 발견 건수, 컬렉션)
- ✅ 대시보드 "최근 업로드" 섹션만 제거
- ✅ 컬렉션을 폴더처럼 표시
- ✅ 콘텐츠를 파일처럼 표시
- ✅ 계층적 탐색 지원

---

## 2. 현재 상황 분석

### 2.1 현재 라우팅 구조
```
/(user)
├── dashboard/            # 대시보드 (통계 + 최근 업로드)
├── contents/            # 콘텐츠 목록
│   ├── [id]/           # 콘텐츠 상세
│   └── upload/         # 업로드
└── collections/         # 컬렉션 목록
```

### 2.2 현재 레이아웃
```
┌─────────────────────────────────────────┐
│           Header                        │
├──────────┬──────────────────────────────┤
│          │                              │
│ Sidebar  │    Main Content              │
│          │    (Dashboard, Contents,     │
│ - 대시보드 │     Collections)             │
│ - 콘텐츠   │                              │
│ - 컬렉션   │                              │
│          │                              │
└──────────┴──────────────────────────────┘
```

### 2.3 문제점
1. 컬렉션과 콘텐츠가 분리되어 있어 관계 파악 어려움
2. 페이지 간 이동이 빈번하게 발생
3. 대시보드의 "최근 업로드"가 중복 정보 제공 (제거 대상)
4. 사이드바가 고정 공간을 차지하여 콘텐츠 영역 제한

**유지할 요소**:
- 대시보드 상단의 통계 카드 (총 콘텐츠, 분석 완료, 발견 건수, 컬렉션) - 유용한 정보 제공

---

## 3. 목표 아키텍처

### 3.1 새로운 라우팅 구조
```
/(user)
├── page.tsx             # 파일 탐색기 (루트)
│   (?collection=:id)    # 쿼리 파라미터로 컬렉션 선택
│                        # 상단에 통계 카드 포함
├── contents/
│   ├── [id]/           # 콘텐츠 상세 (유지)
│   └── upload/         # 업로드 (유지)
```

### 3.2 새로운 레이아웃
```
┌────────────────────────────────────────────────────────┐
│  Header (Breadcrumb, Search, Upload, User)             │
├────────────────────────────────────────────────────────┤
│  [통계 카드: 총 콘텐츠 | 분석 완료 | 발견 건수 | 컬렉션] │
├──────────────────┬─────────────────────────────────────┤
│                  │                                     │
│  Folder Tree     │    Content View                     │
│  (Resizable)     │    (Grid / List)                    │
│                  │                                     │
│  📁 컬렉션 1      │    [Toolbar: View, Sort, Filter]    │
│  📁 컬렉션 2      │                                     │
│    📁 하위 폴더   │    🖼️ 📄 🖼️ 📄                      │
│  📁 컬렉션 3      │    🖼️ 📄 🖼️ 📄                      │
│                  │    🖼️ 📄 🖼️ 📄                      │
│                  │                                     │
└──────────────────┴─────────────────────────────────────┘
```

### 3.3 레이아웃 특징
- **통계 카드**: 상단에 주요 현황 정보 표시 (기존 대시보드 통계 재사용)
- **좌우 분할**: ResizablePanel로 좌우 비율 조정 가능
- **폴더 트리**: 좌측에 컬렉션 계층 구조 표시
- **콘텐츠 영역**: 우측에 선택된 컬렉션의 콘텐츠 표시
- **통합 툴바**: Breadcrumb, 검색, 뷰 모드 등 제공

---

## 4. 주요 변경사항

### 4.1 생성할 파일
```
app/(user)/
└── page.tsx                              # 파일 탐색기 루트 페이지
                                          # (통계 카드 + 파일 탐색기 통합)

components/explorer/
├── StatsCards.tsx                        # 통계 카드 (대시보드에서 재사용)
├── FolderTree.tsx                        # 폴더 트리 컴포넌트
├── ContentExplorerView.tsx               # 콘텐츠 표시 영역
├── ExplorerToolbar.tsx                   # 도구 모음
├── ExplorerBreadcrumb.tsx                # 경로 표시
└── index.ts                              # Export 파일

lib/stores/
└── explorerStore.ts                      # 탐색기 상태 관리
```

### 4.2 수정할 파일
```
app/(user)/
└── layout.tsx                            # Sidebar 제거, 단순화

components/layout/
└── Header.tsx                            # Breadcrumb 통합 (선택사항)
```

### 4.3 제거할 파일
```
app/(user)/
├── dashboard/page.tsx                    # 루트로 통합 (통계 카드는 재사용)
├── contents/page.tsx                     # 루트로 통합
└── collections/page.tsx                  # 루트로 통합

components/layout/
└── Sidebar.tsx                           # 더 이상 사용 안 함
```

### 4.4 재사용할 요소
- **통계 카드 컴포넌트**: 기존 `dashboard/page.tsx`의 StatCard를 독립 컴포넌트로 분리하여 재사용
- **통계 데이터 로직**: 총 콘텐츠, 분석 완료, 발견 건수, 컬렉션 수 계산 로직

---

## 5. 컴포넌트 설계

### 5.1 StatsCards 컴포넌트

**책임**:
- 주요 통계 정보를 카드 형태로 표시
- 기존 대시보드의 StatCard 재사용

**Props**:
```typescript
interface StatsCardsProps {
  totalContents: number
  completedAnalysis: number
  totalDetections: number
  totalCollections: number
}
```

**주요 기능**:
- 4개의 통계 카드 그리드 레이아웃
- 아이콘 및 색상 구분
- 반응형 레이아웃 (모바일: 2x2, 데스크톱: 1x4)

**재사용 요소**:
```typescript
// 기존 dashboard/page.tsx의 StatCard를 독립 컴포넌트로 추출
interface StatCardProps {
  title: string
  value: number
  icon: React.ReactNode
  description: string
}
```

---

### 5.2 FolderTree 컴포넌트

**책임**:
- 컬렉션을 폴더 트리 구조로 표시
- 폴더 선택 상태 관리
- 폴더 확장/축소
- 컨텍스트 메뉴 (새 폴더, 이름 변경, 삭제)

**Props**:
```typescript
interface FolderTreeProps {
  collections: Collection[]
  selectedId?: string
  onSelectCollection: (id: string | null) => void
  onCreateCollection?: () => void
  onRenameCollection?: (id: string) => void
  onDeleteCollection?: (id: string) => void
}
```

**주요 기능**:
- 📁 폴더 아이콘 표시
- 🔽 확장/축소 아이콘
- 선택 시 배경색 변경
- 우클릭 컨텍스트 메뉴

---

### 5.2 ContentExplorerView 컴포넌트

**책임**:
- 선택된 컬렉션의 콘텐츠 표시
- 그리드/리스트 뷰 지원
- 콘텐츠 선택 및 다중 선택
- 드래그 앤 드롭 대상

**Props**:
```typescript
interface ContentExplorerViewProps {
  contents: Content[]
  viewMode: 'grid' | 'list'
  selectedIds: string[]
  onSelectContent: (ids: string[]) => void
  onOpenContent: (id: string) => void
  onDropContent?: (contentIds: string[], targetCollectionId: string) => void
}
```

**주요 기능**:
- 그리드 뷰: 썸네일 카드
- 리스트 뷰: 테이블 형식
- 다중 선택 (Ctrl/Cmd + Click, Shift + Click)
- 우클릭 컨텍스트 메뉴
- 드래그 앤 드롭 수신

---

### 5.3 ExplorerToolbar 컴포넌트

**책임**:
- 뷰 모드 전환 (그리드/리스트)
- 정렬 옵션
- 검색 기능
- 빠른 작업 버튼

**Props**:
```typescript
interface ExplorerToolbarProps {
  viewMode: 'grid' | 'list'
  onViewModeChange: (mode: 'grid' | 'list') => void
  sortBy: SortOption
  onSortChange: (sort: SortOption) => void
  searchQuery: string
  onSearchChange: (query: string) => void
}
```

**주요 기능**:
- 🔳 / 📋 뷰 모드 토글
- 정렬: 이름, 날짜, 크기, 발견 건수
- 검색 입력 필드
- 업로드 버튼 (Header로 이동 가능)

---

### 5.4 ExplorerBreadcrumb 컴포넌트

**책임**:
- 현재 위치 표시
- 클릭하여 상위 경로 이동

**Props**:
```typescript
interface ExplorerBreadcrumbProps {
  currentCollection?: Collection
  onNavigate: (collectionId: string | null) => void
}
```

**표시 예시**:
```
홈 > 컬렉션 1 > 하위 폴더
```

---

## 6. 구현 단계

### Phase 1: 준비 및 기본 구조 (1-2시간)

#### Step 1: shadcn/ui 컴포넌트 설치
```bash
npx shadcn-ui@latest add resizable
npx shadcn-ui@latest add breadcrumb
npx shadcn-ui@latest add context-menu
```

#### Step 2: 상태 관리 스토어 생성
`lib/stores/explorerStore.ts`:
```typescript
import { create } from 'zustand'

interface ExplorerStore {
  selectedCollectionId: string | null
  selectedContentIds: string[]
  viewMode: 'grid' | 'list'
  sortBy: 'name' | 'date' | 'size' | 'detections'
  searchQuery: string

  setSelectedCollection: (id: string | null) => void
  setSelectedContents: (ids: string[]) => void
  toggleViewMode: () => void
  setSortBy: (sort: string) => void
  setSearchQuery: (query: string) => void
}
```

---

### Phase 2: 핵심 컴포넌트 구현 (3-4시간)

#### Step 3: StatsCards 컴포넌트 생성
- 기존 dashboard/page.tsx에서 StatCard 함수 추출
- components/explorer/StatsCards.tsx로 독립 컴포넌트화
- 4개 통계 카드 그리드 레이아웃
- 반응형 디자인 적용

#### Step 4: FolderTree 구현
- 컬렉션 데이터를 트리 구조로 변환
- 재귀적 렌더링 (중첩 폴더 지원)
- 선택 상태 시각화
- 확장/축소 애니메이션

#### Step 5: ContentExplorerView 구현
- 그리드 뷰 레이아웃 (CSS Grid)
- 리스트 뷰 레이아웃 (Table)
- 선택 로직 (단일, 다중, 범위 선택)
- 빈 상태 처리

#### Step 6: ExplorerToolbar 구현
- shadcn/ui Select 사용
- SearchInput 재사용
- 버튼 그룹 레이아웃

#### Step 7: ExplorerBreadcrumb 구현
- shadcn/ui Breadcrumb 사용
- 동적 경로 생성

---

### Phase 3: 메인 페이지 통합 (2-3시간)

#### Step 8: 루트 페이지 생성
`app/(user)/page.tsx`:
```typescript
'use client'

import { useState, useEffect } from 'react'
import { ResizablePanelGroup, ResizablePanel, ResizableHandle } from '@/components/ui/resizable'
import { StatsCards, FolderTree, ContentExplorerView, ExplorerToolbar } from '@/components/explorer'
import { useExplorerStore } from '@/lib/stores/explorerStore'

export default function ExplorerPage() {
  // 통계 데이터 계산 (기존 dashboard 로직 재사용)
  // 상태 관리
  // 컴포넌트 렌더링:
  //   1. StatsCards (상단)
  //   2. ResizablePanel (좌: FolderTree, 우: ContentExplorerView + Toolbar)
}
```

#### Step 9: 레이아웃 수정
`app/(user)/layout.tsx`:
- Sidebar 관련 코드 제거
- sidebarOpen 상태 제거
- 전체 너비 main 레이아웃

---

### Phase 4: 고급 기능 (2-3시간)

#### Step 10: 컨텍스트 메뉴
- ContextMenu 컴포넌트 사용
- 폴더: 새 폴더, 이름 변경, 삭제
- 콘텐츠: 열기, 이동, 삭제, 다운로드

#### Step 11: 드래그 앤 드롭
- HTML5 Drag & Drop API
- 드래그 시작: 콘텐츠
- 드롭 대상: 폴더
- 시각적 피드백 (드래그 중 스타일)

---

### Phase 5: 정리 및 테스트 (1-2시간)

#### Step 12: 기존 페이지 제거
```bash
rm app/(user)/dashboard/page.tsx   # StatCard 로직은 StatsCards 컴포넌트로 이미 추출됨
rm app/(user)/contents/page.tsx
rm app/(user)/collections/page.tsx
rm components/layout/Sidebar.tsx
```

#### Step 13: 문서 업데이트
- FOLDER_STRUCTURE.md 수정
- 새로운 컴포넌트 구조 반영

#### Step 14: Playwright 테스트
- 통계 카드 표시 확인
- 폴더 선택 및 콘텐츠 표시
- 검색 및 정렬
- 뷰 모드 전환
- 드래그 앤 드롭

---

## 7. 데이터 흐름

### 7.1 초기 로드
```
1. ExplorerPage 마운트
2. useEffect에서 컬렉션 및 콘텐츠 로드
3. explorerStore 초기화
4. FolderTree에 컬렉션 전달
5. ContentExplorerView에 필터링된 콘텐츠 전달
```

### 7.2 폴더 선택
```
1. 사용자가 FolderTree에서 폴더 클릭
2. onSelectCollection 콜백 호출
3. explorerStore.setSelectedCollection(id) 호출
4. 상태 변경으로 ContentExplorerView 리렌더링
5. 선택된 컬렉션의 콘텐츠 필터링 및 표시
```

### 7.3 검색 및 필터링
```
1. 사용자가 ExplorerToolbar에서 검색어 입력
2. explorerStore.setSearchQuery(query) 호출
3. useMemo로 콘텐츠 필터링
4. ContentExplorerView에 필터링된 결과 전달
```

### 7.4 드래그 앤 드롭
```
1. 사용자가 콘텐츠를 드래그 시작
2. onDragStart에서 dataTransfer에 contentId 저장
3. 폴더 위로 드래그 시 onDragOver 처리
4. 폴더에 드롭 시 onDrop 호출
5. API 호출하여 콘텐츠의 collection_id 업데이트
6. 상태 갱신 및 UI 반영
```

---

## 8. UI/UX 가이드라인

### 8.1 레이아웃
- **좌측 패널 (FolderTree)**: 최소 200px, 기본 280px, 최대 400px
- **우측 패널 (ContentView)**: 나머지 공간
- **Resizable Handle**: 드래그 가능한 구분선 (4px)

### 8.2 시각적 피드백
- **선택된 폴더**: `bg-primary-50 text-primary-600`
- **Hover 상태**: `hover:bg-secondary-50`
- **드래그 중**: 반투명 오버레이 및 커서 변경
- **드롭 가능 영역**: 파란색 테두리 또는 배경색 변경

### 8.3 반응형 디자인
- **Mobile (< 640px)**: 폴더 트리를 모달/드로어로 변경
- **Tablet (640-1024px)**: 패널 비율 조정
- **Desktop (> 1024px)**: 기본 레이아웃

### 8.4 아이콘 사용
- 📁 **FolderOpen**: 폴더 (lucide-react)
- 🖼️ **FileImage**: 이미지 콘텐츠
- 🔽 **ChevronDown**: 확장된 폴더
- ▶️ **ChevronRight**: 축소된 폴더
- 🔳 **Grid**: 그리드 뷰
- 📋 **List**: 리스트 뷰

### 8.5 애니메이션
- 폴더 확장/축소: `transition-all duration-200`
- 콘텐츠 로딩: Skeleton 사용
- 드래그 앤 드롭: `transition-opacity duration-150`

---

## 9. 테스트 계획

### 9.1 단위 테스트 (선택사항)
- FolderTree 렌더링
- ContentExplorerView 필터링 로직
- explorerStore 상태 변경

### 9.2 통합 테스트
- 폴더 선택 → 콘텐츠 표시
- 검색 → 필터링된 결과
- 정렬 → 순서 변경

### 9.3 E2E 테스트 (Playwright)

**시나리오 1: 기본 탐색**
```typescript
test('파일 탐색기 기본 탐색', async ({ page }) => {
  await page.goto('/')

  // 폴더 트리 확인
  await expect(page.locator('[data-testid="folder-tree"]')).toBeVisible()

  // 폴더 선택
  await page.click('text=컬렉션 1')

  // 콘텐츠 표시 확인
  await expect(page.locator('[data-testid="content-grid"]')).toBeVisible()
})
```

**시나리오 2: 검색 및 필터링**
```typescript
test('검색 기능', async ({ page }) => {
  await page.goto('/')

  // 검색어 입력
  await page.fill('[placeholder="콘텐츠 검색..."]', '테스트')

  // 검색 결과 확인
  const results = page.locator('[data-testid="content-item"]')
  await expect(results).toHaveCount(3) // 예상 결과 수
})
```

**시나리오 3: 뷰 모드 전환**
```typescript
test('그리드/리스트 뷰 전환', async ({ page }) => {
  await page.goto('/')

  // 그리드 뷰 확인
  await expect(page.locator('[data-testid="grid-view"]')).toBeVisible()

  // 리스트 뷰로 전환
  await page.click('[data-testid="list-view-toggle"]')

  // 리스트 뷰 확인
  await expect(page.locator('[data-testid="list-view"]')).toBeVisible()
})
```

**시나리오 4: 드래그 앤 드롭** (선택사항)
```typescript
test('콘텐츠 드래그 앤 드롭으로 폴더 이동', async ({ page }) => {
  await page.goto('/')

  // 콘텐츠 드래그
  const content = page.locator('[data-testid="content-item"]').first()
  const targetFolder = page.locator('text=컬렉션 2')

  await content.dragTo(targetFolder)

  // 이동 확인
  await page.click('text=컬렉션 2')
  await expect(page.locator('[data-testid="content-item"]')).toContainText('이동된 콘텐츠')
})
```

---

## 10. 예상 문제 및 해결 방안

### 10.1 성능 문제
**문제**: 많은 수의 컬렉션/콘텐츠로 인한 렌더링 지연

**해결**:
- Virtual Scrolling (react-window 또는 react-virtualized)
- 페이지네이션 또는 무한 스크롤
- useMemo로 필터링 결과 캐싱

### 10.2 드래그 앤 드롭 호환성
**문제**: 터치 디바이스에서 드래그 앤 드롭 미지원

**해결**:
- 모바일에서는 "이동" 버튼 사용
- 또는 react-dnd의 터치 백엔드 사용

### 10.3 상태 동기화
**문제**: 여러 탭에서 동시 작업 시 상태 불일치

**해결**:
- Supabase Realtime으로 실시간 동기화
- 또는 polling으로 주기적 갱신

---

## 11. 향후 개선 사항

### 11.1 단기 (MVP 이후)
- 북마크/즐겨찾기 폴더
- 최근 방문한 폴더 기록
- 키보드 단축키 (방향키, Enter, Delete 등)

### 11.2 중기
- 폴더 간 복사 기능
- 다중 파일 선택 후 일괄 작업
- 상세 정보 패널 (우측 사이드바)

### 11.3 장기
- 태그 시스템
- 스마트 폴더 (자동 필터링)
- 썸네일 크기 조정
- 컬러 라벨

---

## 12. 참고 자료

### 12.1 디자인 참고
- Windows 11 File Explorer
- macOS Finder
- Google Drive
- Dropbox

### 12.2 기술 문서
- [shadcn/ui Resizable](https://ui.shadcn.com/docs/components/resizable)
- [shadcn/ui Breadcrumb](https://ui.shadcn.com/docs/components/breadcrumb)
- [HTML5 Drag and Drop](https://developer.mozilla.org/en-US/docs/Web/API/HTML_Drag_and_Drop_API)
- [Zustand Documentation](https://docs.pmnd.rs/zustand/getting-started/introduction)

---

## 13. 체크리스트

### 구현 완료 체크리스트
- [ ] shadcn/ui 컴포넌트 설치
- [ ] explorerStore 생성
- [ ] **StatsCards 컴포넌트** (대시보드에서 StatCard 추출 및 독립화)
- [ ] FolderTree 컴포넌트
- [ ] ContentExplorerView 컴포넌트
- [ ] ExplorerToolbar 컴포넌트
- [ ] ExplorerBreadcrumb 컴포넌트
- [ ] 루트 페이지 생성 (통계 카드 + 파일 탐색기 통합)
- [ ] 레이아웃 수정 (Sidebar 제거)
- [ ] 컨텍스트 메뉴 구현
- [ ] 드래그 앤 드롭 구현
- [ ] 기존 페이지 제거 (dashboard 통계는 StatsCards로 재사용됨)
- [ ] FOLDER_STRUCTURE.md 업데이트
- [ ] Playwright E2E 테스트 (통계 카드 표시 확인 포함)
- [ ] 반응형 레이아웃 테스트
- [ ] 크로스 브라우저 테스트

---

_이 문서는 태스크 7, 8번 구현을 위한 상세 계획서입니다. 구현 중 발견된 이슈나 개선사항은 지속적으로 업데이트됩니다._
