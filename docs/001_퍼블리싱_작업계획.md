# UTRBOX 퍼블리싱 작업 계획서

**문서 번호**: 001
**작성일**: 2025-10-23
**작성자**: Claude
**버전**: 1.0.0

---

## 📋 목차

1. [작업 개요](#1-작업-개요)
2. [기술 스택](#2-기술-스택)
3. [디자인 시스템](#3-디자인-시스템)
4. [Mock 데이터 구조](#4-mock-데이터-구조)
5. [컴포넌트 설계](#5-컴포넌트-설계)
6. [페이지 구현 계획](#6-페이지-구현-계획)
7. [작업 순서](#7-작업-순서)
8. [체크리스트](#8-체크리스트)

---

## 1. 작업 개요

### 1.1 목적
UTRBOX 콘텐츠 저작권 관리 시스템의 사용자 포털 UI를 Mock 데이터 기반으로 우선 구현하여, 실제 백엔드 연동 전에 UI/UX를 검증하고 개선합니다.

### 1.2 작업 범위

**포함 사항**:
- ✅ Next.js 14 기반 프로젝트 초기 환경 구축
- ✅ shadcn/ui를 활용한 디자인 시스템 구축
- ✅ TypeScript 타입 정의 및 Mock 데이터 준비
- ✅ 사용자 포털 주요 페이지 퍼블리싱
- ✅ 반응형 디자인 적용
- ✅ Mock API를 통한 데이터 바인딩

**제외 사항**:
- ❌ 실제 백엔드 API 연동
- ❌ Supabase 데이터베이스 연동
- ❌ Google Vision API 연동
- ❌ 관리자 포털 (2차 작업으로 분리)

### 1.3 작업 원칙

1. **Mock 데이터 우선**: 모든 UI는 Mock 데이터로 동작해야 함
2. **반응형 필수**: 모바일, 태블릿, 데스크톱 모두 지원
3. **타입 안정성**: TypeScript를 활용한 타입 안정성 확보
4. **구조 준수**: FOLDER_STRUCTURE.md에 정의된 디렉터리 구조 준수
5. **컴포넌트 재사용**: shadcn/ui 및 공통 컴포넌트 최대 활용

---

## 2. 기술 스택

### 2.1 Core
- **Framework**: Next.js 14.x (App Router)
- **Language**: TypeScript 5.x
- **Runtime**: Node.js 20.x LTS

### 2.2 Styling
- **CSS Framework**: Tailwind CSS 3.x
- **UI Components**: shadcn/ui
- **Icons**: lucide-react

### 2.3 State Management
- **Global State**: Zustand
- **Form State**: React Hook Form + Zod

### 2.4 Development
- **Linting**: ESLint
- **Formatting**: Prettier
- **Type Checking**: TypeScript strict mode

---

## 3. 디자인 시스템

### 3.1 색상 팔레트

PRD에 정의된 색상을 Tailwind CSS에 적용:

```typescript
// tailwind.config.ts
colors: {
  primary: {
    DEFAULT: '#3B82F6',  // Blue
    50: '#EFF6FF',
    100: '#DBEAFE',
    200: '#BFDBFE',
    300: '#93C5FD',
    400: '#60A5FA',
    500: '#3B82F6',  // Base
    600: '#2563EB',
    700: '#1D4ED8',
    800: '#1E40AF',
    900: '#1E3A8A',
  },
  secondary: {
    DEFAULT: '#6B7280',  // Gray
    50: '#F9FAFB',
    100: '#F3F4F6',
    200: '#E5E7EB',
    300: '#D1D5DB',
    400: '#9CA3AF',
    500: '#6B7280',  // Base
    600: '#4B5563',
    700: '#374151',
    800: '#1F2937',
    900: '#111827',
  },
  success: '#10B981',  // Green
  warning: '#F59E0B',  // Yellow
  error: '#EF4444',    // Red
}
```

### 3.2 타이포그래피

```typescript
// tailwind.config.ts
fontFamily: {
  sans: ['Inter', 'Noto Sans KR', 'sans-serif'],
},
fontSize: {
  'heading-1': '32px',
  'heading-2': '24px',
  'heading-3': '20px',
  'heading-4': '16px',
  'body-lg': '16px',
  'body': '14px',
  'body-sm': '12px',
}
```

### 3.3 간격 시스템

4px 기반 그리드 시스템:
- Spacing: 4, 8, 12, 16, 20, 24, 32, 40, 48, 64px
- Border Radius: 4, 8, 12, 16, 24px

### 3.4 반응형 브레이크포인트

```typescript
screens: {
  'sm': '640px',   // Mobile
  'md': '768px',   // Tablet
  'lg': '1024px',  // Desktop
  'xl': '1280px',  // Large Desktop
  '2xl': '1536px', // Extra Large
}
```

---

## 4. Mock 데이터 구조

### 4.1 사용자 (User)

```typescript
// types/user.ts
export type UserRole = 'Admin' | 'Member';
export type UserStatus = 'pending' | 'approved' | 'blocked';

export interface User {
  id: string;
  email: string;
  name: string;
  organization: string;
  role: UserRole;
  status: UserStatus;
  avatar_url?: string;
  created_at: string;
  updated_at: string;
}
```

**Mock 데이터 샘플** (`lib/mock-data/users.ts`):
- 총 10명의 사용자
- Admin 2명, Member 8명
- 다양한 상태 (승인, 대기, 차단)

### 4.2 콘텐츠 (Content)

```typescript
// types/content.ts
export type AnalysisStatus = 'pending' | 'analyzing' | 'completed' | 'failed';

export interface Content {
  id: string;
  user_id: string;
  collection_id: string | null;
  title: string;
  description: string;
  file_url: string;
  thumbnail_url: string;
  file_size: number;
  file_type: 'jpg' | 'png';
  analysis_status: AnalysisStatus;
  detection_count: number;
  upload_date: string;
  updated_at: string;
}
```

**Mock 데이터 샘플** (`lib/mock-data/contents.ts`):
- 총 30개의 콘텐츠
- Unsplash 이미지 활용
- 다양한 분석 상태 및 발견 건수

### 4.3 컬렉션 (Collection)

```typescript
// types/collection.ts
export interface Collection {
  id: string;
  user_id: string;
  name: string;
  description: string;
  item_count: number;
  created_at: string;
  updated_at: string;
}
```

**Mock 데이터 샘플** (`lib/mock-data/collections.ts`):
- 총 5개의 컬렉션
- 다양한 콘텐츠 수

### 4.4 분석 결과 (AnalysisResult)

```typescript
// types/analysis.ts
export interface Label {
  description: string;
  score: number;
}

export interface AnalysisResult {
  id: string;
  content_id: string;
  labels: Label[];
  texts: string[];
  full_matching_images: string[];
  partial_matching_images: string[];
  similar_images: string[];
  analyzed_at: string;
}
```

**Mock 데이터 샘플** (`lib/mock-data/analysisResults.ts`):
- Google Vision API 응답 형식 모방
- 라벨, 텍스트 샘플

### 4.5 발견 콘텐츠 (DetectedContent)

```typescript
// types/detection.ts
export type ReviewStatus = 'pending' | 'match' | 'no_match' | 'unclear';

export interface DetectedContent {
  id: string;
  original_content_id: string;
  detected_url: string;
  similarity_score: number;
  detection_type: 'full' | 'partial' | 'similar';
  review_status: ReviewStatus;
  reviewed_by?: string;
  reviewed_at?: string;
  review_note?: string;
  detected_at: string;
}
```

**Mock 데이터 샘플** (`lib/mock-data/detectedContents.ts`):
- 다양한 유사도 점수
- 다양한 검토 상태

---

## 5. 컴포넌트 설계

### 5.1 shadcn/ui 기본 컴포넌트

설치할 shadcn/ui 컴포넌트:

```bash
npx shadcn-ui@latest add button
npx shadcn-ui@latest add card
npx shadcn-ui@latest add dialog
npx shadcn-ui@latest add dropdown-menu
npx shadcn-ui@latest add form
npx shadcn-ui@latest add input
npx shadcn-ui@latest add label
npx shadcn-ui@latest add select
npx shadcn-ui@latest add table
npx shadcn-ui@latest add tabs
npx shadcn-ui@latest add toast
npx shadcn-ui@latest add avatar
npx shadcn-ui@latest add badge
npx shadcn-ui@latest add checkbox
npx shadcn-ui@latest add progress
npx shadcn-ui@latest add separator
npx shadcn-ui@latest add skeleton
```

### 5.2 공통 컴포넌트

#### LoadingSpinner
**위치**: `components/common/LoadingSpinner.tsx`
**목적**: 로딩 상태 표시
**Props**: `size?: 'sm' | 'md' | 'lg'`, `text?: string`

#### EmptyState
**위치**: `components/common/EmptyState.tsx`
**목적**: 빈 상태 UI
**Props**: `icon`, `title`, `description`, `action?`

#### Pagination
**위치**: `components/common/Pagination.tsx`
**목적**: 페이지네이션 컴포넌트
**Props**: `currentPage`, `totalPages`, `onPageChange`

#### SearchInput
**위치**: `components/common/SearchInput.tsx`
**목적**: 검색 입력 필드
**Props**: `placeholder`, `value`, `onChange`, `onSearch`

#### ConfirmDialog
**위치**: `components/common/ConfirmDialog.tsx`
**목적**: 확인 대화상자
**Props**: `open`, `title`, `description`, `onConfirm`, `onCancel`

#### ImageViewer
**위치**: `components/common/ImageViewer.tsx`
**목적**: 이미지 확대/축소 뷰어
**Props**: `src`, `alt`, `onClose`

### 5.3 레이아웃 컴포넌트

#### Header
**위치**: `components/layout/Header.tsx`
**기능**:
- 로고
- 네비게이션 메뉴
- 사용자 프로필 드롭다운 (로그아웃)
- 반응형 햄버거 메뉴 (모바일)

#### Sidebar
**위치**: `components/layout/Sidebar.tsx`
**기능**:
- 메뉴 아이템 (대시보드, 콘텐츠, 컬렉션, 발견 결과)
- 활성 상태 표시
- 아이콘 + 텍스트
- 접기/펼치기 기능

#### Footer
**위치**: `components/layout/Footer.tsx`
**기능**:
- 저작권 정보
- 링크 (이용약관, 개인정보처리방침)

### 5.4 인증 컴포넌트

#### LoginForm
**위치**: `components/auth/LoginForm.tsx`
**기능**:
- 이메일/비밀번호 입력
- Remember Me 체크박스
- 로그인 버튼
- Validation (React Hook Form + Zod)

#### SignupForm
**위치**: `components/auth/SignupForm.tsx`
**기능**:
- 이메일, 비밀번호, 이름, 소속 입력
- 약관 동의 체크박스
- 회원가입 버튼
- Validation

#### ResetPasswordForm
**위치**: `components/auth/ResetPasswordForm.tsx`
**기능**:
- 이메일 입력
- 재설정 링크 발송 버튼

### 5.5 콘텐츠 컴포넌트

#### ContentCard
**위치**: `components/content/ContentCard.tsx`
**기능**:
- 썸네일 이미지
- 제목, 업로드 날짜
- 분석 상태 배지
- 발견 건수 표시
- 클릭 시 상세 페이지 이동

#### ContentGrid
**위치**: `components/content/ContentGrid.tsx`
**기능**:
- ContentCard 그리드 레이아웃
- 반응형 (1열 → 2열 → 3열 → 4열)

#### ContentList
**위치**: `components/content/ContentList.tsx`
**기능**:
- 테이블 형식 리스트
- 정렬 가능한 헤더
- 체크박스 선택

#### ContentUploader
**위치**: `components/content/ContentUploader.tsx`
**기능**:
- 드래그 앤 드롭 영역 (react-dropzone)
- 파일 미리보기
- 업로드 진행률 (Mock)
- 파일 타입/크기 검증

#### ContentFilters
**위치**: `components/content/ContentFilters.tsx`
**기능**:
- 정렬 선택 (업로드일, 이름, 발견 건수)
- 상태 필터 (전체, 분석 중, 완료)
- 날짜 범위 선택
- 컬렉션 필터

---

## 6. 페이지 구현 계획

### 6.1 인증 페이지

#### 로그인 페이지
**경로**: `/login`
**파일**: `app/(auth)/login/page.tsx`

**UI 구성**:
```
┌─────────────────────────────────────┐
│          UTRBOX 로고                 │
├─────────────────────────────────────┤
│   이메일 입력                        │
│   비밀번호 입력                      │
│   □ 로그인 상태 유지                 │
│   [로그인 버튼]                      │
├─────────────────────────────────────┤
│   비밀번호 찾기 | 회원가입           │
└─────────────────────────────────────┘
```

**Mock 동작**:
- 이메일/비밀번호 입력 시 Mock 사용자 조회
- 로그인 성공 시 `/dashboard`로 리다이렉트
- Zustand에 사용자 정보 저장

#### 회원가입 페이지
**경로**: `/signup`
**파일**: `app/(auth)/signup/page.tsx`

**UI 구성**:
```
┌─────────────────────────────────────┐
│          회원가입                    │
├─────────────────────────────────────┤
│   이메일 입력                        │
│   비밀번호 입력                      │
│   비밀번호 확인                      │
│   이름 입력                          │
│   소속 입력                          │
│   □ 이용약관 동의 (필수)             │
│   □ 개인정보처리방침 동의 (필수)     │
│   [회원가입 버튼]                    │
└─────────────────────────────────────┘
```

**Mock 동작**:
- 회원가입 성공 시 승인 대기 모달 표시
- 로그인 페이지로 리다이렉트

#### 비밀번호 재설정 페이지
**경로**: `/reset-password`
**파일**: `app/(auth)/reset-password/page.tsx`

**UI 구성**:
```
┌─────────────────────────────────────┐
│     비밀번호 재설정                  │
├─────────────────────────────────────┤
│   등록된 이메일 주소를 입력하세요    │
│   이메일 입력                        │
│   [재설정 링크 발송]                 │
└─────────────────────────────────────┘
```

### 6.2 사용자 포털 - 대시보드

**경로**: `/dashboard`
**파일**: `app/(user)/dashboard/page.tsx`

**UI 구성**:
```
┌─────────────────────────────────────┐
│ Header (로고, 네비, 사용자 메뉴)     │
├──────┬──────────────────────────────┤
│      │  대시보드                     │
│      ├──────────────────────────────┤
│      │  ┌─────┐ ┌─────┐ ┌─────┐    │
│ Side │  │총콘텐│ │분석완│ │발견건│    │
│ bar  │  │  30 │ │  25 │ │  12 │    │
│      │  └─────┘ └─────┘ └─────┘    │
│      ├──────────────────────────────┤
│      │  최근 업로드                  │
│      │  [콘텐츠 카드 1]              │
│      │  [콘텐츠 카드 2]              │
│      │  [콘텐츠 카드 3]              │
│      ├──────────────────────────────┤
│      │  최근 활동                    │
│      │  - 콘텐츠 업로드 (2시간 전)   │
│      │  - 분석 완료 (5시간 전)       │
└──────┴──────────────────────────────┘
```

**컴포넌트**:
- `StatsCard`: 통계 카드
- `RecentActivity`: 최근 활동 목록

### 6.3 사용자 포털 - 콘텐츠 목록

**경로**: `/contents`
**파일**: `app/(user)/contents/page.tsx`

**UI 구성**:
```
┌─────────────────────────────────────┐
│ Header                               │
├──────┬──────────────────────────────┤
│      │  콘텐츠 관리                  │
│      ├──────────────────────────────┤
│      │ [검색] [필터] [정렬] [뷰전환] │
│ Side ├──────────────────────────────┤
│ bar  │  ┌───┐ ┌───┐ ┌───┐ ┌───┐    │
│      │  │img│ │img│ │img│ │img│    │
│      │  └───┘ └───┘ └───┘ └───┘    │
│      │  ┌───┐ ┌───┐ ┌───┐ ┌───┐    │
│      │  │img│ │img│ │img│ │img│    │
│      │  └───┘ └───┘ └───┘ └───┘    │
│      ├──────────────────────────────┤
│      │  [페이지네이션]               │
└──────┴──────────────────────────────┘
```

**기능**:
- 그리드/리스트 뷰 전환
- 검색 (제목, 설명)
- 필터 (상태, 컬렉션, 날짜)
- 정렬 (업로드일, 이름, 발견 건수)
- 페이지네이션 (20개/페이지)

### 6.4 사용자 포털 - 콘텐츠 업로드

**경로**: `/contents/upload`
**파일**: `app/(user)/contents/upload/page.tsx`

**UI 구성**:
```
┌─────────────────────────────────────┐
│ Header                               │
├──────┬──────────────────────────────┤
│      │  콘텐츠 업로드                │
│      ├──────────────────────────────┤
│      │  ┌─────────────────────────┐ │
│ Side │  │  드래그 앤 드롭 영역     │ │
│ bar  │  │  또는 클릭하여 파일 선택 │ │
│      │  └─────────────────────────┘ │
│      │                               │
│      │  컬렉션 선택: [드롭다운]     │
│      │                               │
│      │  [미리보기]                   │
│      │  파일1.jpg (2.5MB) [X]       │
│      │  ███████░░░ 70%              │
│      │                               │
│      │  [업로드 시작] [취소]         │
└──────┴──────────────────────────────┘
```

**기능**:
- react-dropzone으로 드래그 앤 드롭
- 파일 타입 검증 (JPG, PNG)
- 파일 크기 검증 (10MB 이하)
- 다중 파일 업로드
- 업로드 진행률 표시 (Mock)
- 컬렉션 선택

### 6.5 사용자 포털 - 콘텐츠 상세

**경로**: `/contents/[id]`
**파일**: `app/(user)/contents/[id]/page.tsx`

**UI 구성**:
```
┌─────────────────────────────────────┐
│ Header                               │
├──────┬──────────────────────────────┤
│      │  ← 콘텐츠 상세                │
│      ├──────────────────────────────┤
│      │  ┌────────────────┐          │
│ Side │  │                │          │
│ bar  │  │  이미지 뷰어    │          │
│      │  │                │          │
│      │  └────────────────┘          │
│      │                               │
│      │  제목: sample.jpg             │
│      │  크기: 2.5MB                  │
│      │  형식: JPG                    │
│      │  업로드: 2025-10-20           │
│      │  상태: [분석 완료]             │
│      │                               │
│      │  발견된 콘텐츠 (12건)         │
│      │  [발견 항목 1]                │
│      │  [발견 항목 2]                │
└──────┴──────────────────────────────┘
```

**기능**:
- 이미지 확대/축소
- 메타데이터 표시
- 분석 상태 배지
- 발견된 콘텐츠 목록
- 다운로드 버튼

### 6.6 사용자 포털 - 컬렉션 관리

**경로**: `/collections`
**파일**: `app/(user)/collections/page.tsx`

**UI 구성**:
```
┌─────────────────────────────────────┐
│ Header                               │
├──────┬──────────────────────────────┤
│      │  컬렉션 관리                  │
│      │  [+ 새 컬렉션]                │
│      ├──────────────────────────────┤
│      │  ┌───────┐ ┌───────┐        │
│ Side │  │📁여행  │ │📁제품  │        │
│ bar  │  │12개   │ │8개    │        │
│      │  └───────┘ └───────┘        │
│      │  ┌───────┐ ┌───────┐        │
│      │  │📁행사  │ │📁기타  │        │
│      │  │5개    │ │3개    │        │
│      │  └───────┘ └───────┘        │
└──────┴──────────────────────────────┘
```

**기능**:
- 컬렉션 카드 그리드
- 컬렉션 생성 다이얼로그
- 컬렉션 수정/삭제
- 컬렉션 클릭 시 해당 콘텐츠 목록 표시

---

## 7. 작업 순서

### Phase 1: 프로젝트 초기화 (1일차)

1. **Next.js 14 프로젝트 생성**
   ```bash
   npx create-next-app@latest utrbox --typescript --tailwind --app --src-dir=false
   ```

2. **필수 패키지 설치**
   ```bash
   npm install zustand react-hook-form zod @hookform/resolvers
   npm install react-dropzone
   npm install lucide-react
   npm install date-fns
   ```

3. **shadcn/ui 초기화**
   ```bash
   npx shadcn-ui@latest init
   ```

4. **디렉터리 구조 생성**
   - `components/` (ui, auth, content, layout, common, providers)
   - `lib/` (supabase, api, utils, validators, constants, mock-data)
   - `hooks/`
   - `types/`
   - `app/` (auth, user, admin, api)

5. **기본 설정 파일 작성**
   - `tailwind.config.ts`: 색상, 폰트, 간격
   - `tsconfig.json`: 경로 별칭 설정
   - `.env.local`: 환경 변수 (Mock용)

### Phase 2: 타입 및 Mock 데이터 (1일차)

1. **TypeScript 타입 정의**
   - `types/user.ts`
   - `types/content.ts`
   - `types/collection.ts`
   - `types/analysis.ts`
   - `types/detection.ts`
   - `types/api.ts`

2. **Mock 데이터 생성**
   - `lib/mock-data/users.ts`
   - `lib/mock-data/contents.ts`
   - `lib/mock-data/collections.ts`
   - `lib/mock-data/analysisResults.ts`
   - `lib/mock-data/detectedContents.ts`

3. **Mock API 함수**
   - `lib/api/mock/auth.ts`
   - `lib/api/mock/contents.ts`
   - `lib/api/mock/collections.ts`
   - `lib/api/mock/analysis.ts`

4. **Zustand 상태 관리**
   - `lib/stores/authStore.ts`
   - `lib/stores/contentStore.ts`

### Phase 3: 디자인 시스템 (1일차)

1. **shadcn/ui 컴포넌트 설치**
   ```bash
   npx shadcn-ui@latest add button card dialog dropdown-menu
   npx shadcn-ui@latest add form input label select table tabs toast
   npx shadcn-ui@latest add avatar badge checkbox progress separator skeleton
   ```

2. **공통 컴포넌트 구현**
   - `LoadingSpinner.tsx`
   - `EmptyState.tsx`
   - `Pagination.tsx`
   - `SearchInput.tsx`
   - `ConfirmDialog.tsx`
   - `ImageViewer.tsx`

3. **Tailwind 색상/폰트 적용**
   - `tailwind.config.ts` 수정
   - `app/globals.css` 스타일 추가

### Phase 4: 레이아웃 (2일차)

1. **레이아웃 컴포넌트**
   - `components/layout/Header.tsx`
   - `components/layout/Sidebar.tsx`
   - `components/layout/Footer.tsx`
   - `components/layout/Navigation.tsx`

2. **App 레이아웃**
   - `app/layout.tsx` (RootLayout)
   - `app/(auth)/layout.tsx` (AuthLayout)
   - `app/(user)/layout.tsx` (UserPortalLayout)

3. **반응형 동작 확인**
   - 모바일: 햄버거 메뉴
   - 태블릿: Sidebar 축소
   - 데스크톱: 전체 레이아웃

### Phase 5: 인증 페이지 (2일차)

1. **로그인 페이지**
   - `components/auth/LoginForm.tsx`
   - `app/(auth)/login/page.tsx`
   - Validation 스키마
   - Mock 로그인 처리

2. **회원가입 페이지**
   - `components/auth/SignupForm.tsx`
   - `app/(auth)/signup/page.tsx`
   - 승인 대기 모달

3. **비밀번호 재설정**
   - `components/auth/ResetPasswordForm.tsx`
   - `app/(auth)/reset-password/page.tsx`

### Phase 6: 대시보드 (3일차)

1. **대시보드 컴포넌트**
   - `app/(user)/dashboard/components/StatsCard.tsx`
   - `app/(user)/dashboard/components/RecentActivity.tsx`

2. **대시보드 페이지**
   - `app/(user)/dashboard/page.tsx`
   - Mock 데이터 연동

### Phase 7: 콘텐츠 관리 (3-4일차)

1. **콘텐츠 컴포넌트**
   - `components/content/ContentCard.tsx`
   - `components/content/ContentGrid.tsx`
   - `components/content/ContentList.tsx`
   - `components/content/ContentFilters.tsx`
   - `components/content/ContentUploader.tsx`

2. **콘텐츠 목록 페이지**
   - `app/(user)/contents/page.tsx`
   - 검색, 필터, 정렬 기능
   - 그리드/리스트 뷰 전환

3. **콘텐츠 업로드 페이지**
   - `app/(user)/contents/upload/page.tsx`
   - react-dropzone 연동
   - Mock 업로드 진행률

4. **콘텐츠 상세 페이지**
   - `app/(user)/contents/[id]/page.tsx`
   - 이미지 뷰어
   - 발견 결과 목록

### Phase 8: 컬렉션 관리 (4일차)

1. **컬렉션 컴포넌트**
   - `components/content/CollectionCard.tsx`

2. **컬렉션 페이지**
   - `app/(user)/collections/page.tsx`
   - 생성/수정/삭제 기능

### Phase 9: 검증 및 최적화 (5일차)

1. **반응형 테스트**
   - 모바일, 태블릿, 데스크톱 확인
   - UI 깨짐 수정

2. **TypeScript 타입 체크**
   ```bash
   npm run type-check
   ```

3. **ESLint/Prettier 적용**
   ```bash
   npm run lint
   npm run format
   ```

4. **성능 최적화**
   - 이미지 레이지 로딩
   - 코드 스플리팅 확인

---

## 8. 체크리스트

### 8.1 환경 구축
- [ ] Next.js 14 프로젝트 생성
- [ ] TypeScript 설정 완료
- [ ] Tailwind CSS 설정 완료
- [ ] shadcn/ui 초기화 완료
- [ ] 디렉터리 구조 생성 완료

### 8.2 디자인 시스템
- [ ] Tailwind 색상 팔레트 적용
- [ ] 타이포그래피 설정
- [ ] shadcn/ui 컴포넌트 16개 설치
- [ ] 공통 컴포넌트 6개 구현

### 8.3 타입 및 Mock 데이터
- [ ] User 타입 정의
- [ ] Content 타입 정의
- [ ] Collection 타입 정의
- [ ] Analysis 타입 정의
- [ ] Detection 타입 정의
- [ ] Mock 사용자 데이터 (10명)
- [ ] Mock 콘텐츠 데이터 (30개)
- [ ] Mock 컬렉션 데이터 (5개)
- [ ] Mock API 함수 구현

### 8.4 레이아웃
- [ ] Header 컴포넌트
- [ ] Sidebar 컴포넌트
- [ ] Footer 컴포넌트
- [ ] RootLayout 구현
- [ ] AuthLayout 구현
- [ ] UserPortalLayout 구현

### 8.5 인증 페이지
- [ ] 로그인 페이지
- [ ] 회원가입 페이지
- [ ] 비밀번호 재설정 페이지
- [ ] Mock 인증 처리

### 8.6 대시보드
- [ ] 통계 카드 컴포넌트
- [ ] 최근 활동 컴포넌트
- [ ] 대시보드 페이지

### 8.7 콘텐츠 관리
- [ ] ContentCard 컴포넌트
- [ ] ContentGrid 컴포넌트
- [ ] ContentList 컴포넌트
- [ ] ContentFilters 컴포넌트
- [ ] ContentUploader 컴포넌트
- [ ] 콘텐츠 목록 페이지
- [ ] 콘텐츠 업로드 페이지
- [ ] 콘텐츠 상세 페이지

### 8.8 컬렉션 관리
- [ ] CollectionCard 컴포넌트
- [ ] 컬렉션 목록 페이지
- [ ] 컬렉션 생성/수정 기능

### 8.9 검증
- [ ] 모바일 반응형 확인
- [ ] 태블릿 반응형 확인
- [ ] 데스크톱 레이아웃 확인
- [ ] TypeScript 에러 없음
- [ ] ESLint 에러 없음
- [ ] 모든 페이지 Mock 데이터로 동작

---

## 9. 예상 이슈 및 해결 방안

### 9.1 이미지 URL
**이슈**: Mock 데이터에 사용할 이미지 URL 필요
**해결**: Unsplash Random Image API 활용
```
https://source.unsplash.com/random/800x600?sig=1
```

### 9.2 파일 업로드
**이슈**: 실제 파일 업로드는 Supabase Storage 필요
**해결**: Mock 업로드로 진행률만 표시, 실제 저장 없음

### 9.3 검색/필터/정렬
**이슈**: 클라이언트 사이드에서 처리 필요
**해결**: JavaScript 배열 메서드 활용 (filter, sort, slice)

### 9.4 페이지네이션
**이슈**: 서버 페이지네이션 불가
**해결**: 클라이언트 사이드 페이지네이션 구현

### 9.5 실시간 업데이트
**이슈**: Supabase Realtime 없음
**해결**: Mock에서는 실시간 기능 제외

---

## 10. 다음 단계 (2차 작업)

1. **관리자 포털 퍼블리싱**
   - 회원 관리 페이지
   - 콘텐츠 검토 페이지
   - AI 분석 관리
   - 비교 모달
   - 통계 대시보드

2. **백엔드 연동**
   - Supabase Auth 연동
   - Supabase Database 연동
   - Supabase Storage 연동
   - Google Vision API 연동

3. **최적화 및 테스트**
   - 성능 최적화
   - E2E 테스트
   - 접근성 개선

---

**작성자**: Claude
**최종 수정**: 2025-10-23
**버전**: 1.0.0
