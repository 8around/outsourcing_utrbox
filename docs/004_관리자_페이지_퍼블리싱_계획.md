# 004. 관리자 페이지 퍼블리싱 계획서

**작성일**: 2025-10-24
**최종 수정일**: 2025-10-24
**버전**: 2.0
**작성자**: Claude Code
**프로젝트**: UTRBOX 콘텐츠 저작권 관리 시스템

## 변경 이력
- **v2.0 (2025-10-24)**: 대시보드 통계 및 활동 피드 수정
  - 통계 카드: 총 콘텐츠, 대기중, 분석중, 분석완료, 회원수 (5개)
  - 활동 피드: 최근 업로드된 대기중인 콘텐츠 + 최근 가입 요청
  - 발견 건수 추이 차트 제거
- **v1.0 (2025-10-24)**: 초기 계획서 작성

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [페이지 구조 설계](#2-페이지-구조-설계)
3. [컴포넌트 설계](#3-컴포넌트-설계)
4. [Mock 데이터 구조](#4-mock-데이터-구조)
5. [작업 단계 및 일정](#5-작업-단계-및-일정)
6. [파일 구조](#6-파일-구조)
7. [디자인 시스템](#7-디자인-시스템)
8. [기술 스택](#8-기술-스택)
9. [주의사항 및 제약사항](#9-주의사항-및-제약사항)

---

## 1. 프로젝트 개요

### 1.1 목적
일반 사용자 영역의 퍼블리싱이 완료된 상태에서, PRD에 명시된 관리자 포털(Admin Portal) 기능의 퍼블리싱 작업을 수행합니다.

### 1.2 범위
- **포함**: 관리자 전용 UI 퍼블리싱, Mock 데이터 기반 화면 구현
- **제외**: 실제 API 연동, 인증 로직, Google Vision API 통합

### 1.3 PRD 참조
- **PRD 3.2**: 관리자 포털 기능 요구사항
- **DATABASE_SCHEMA.md**: 데이터베이스 구조 및 RLS 정책

### 1.4 핵심 기능
1. 관리자 인증 (로그인 페이지)
2. 대시보드 (통계 및 실시간 모니터링)
3. 회원 관리 (리스트, 상세, 승인/거부)
4. 콘텐츠 관리 (리스트, 상세, AI 분석 실행)
5. 비교 검토 (발견 콘텐츠 판정)

---

## 2. 페이지 구조 설계

### 2.1 URL 구조

```
/admin
├── /login                          # 관리자 로그인
├── /dashboard                      # 대시보드 (홈)
├── /users                          # 회원 관리
│   ├── /                          # 회원 리스트
│   └── /[id]                      # 회원 상세
├── /contents                       # 콘텐츠 관리
│   ├── /                          # 콘텐츠 리스트
│   └── /[id]                      # 콘텐츠 상세 + 분석 결과
└── /review                         # 비교 검토
    ├── /                          # 검토 대기 리스트
    └── /[detectionId]             # 개별 비교 검토 (모달)
```

### 2.2 페이지별 기능 정의

#### 2.2.1 로그인 페이지 (`/admin/login`)
- **목적**: 관리자 인증
- **UI 요소**:
  - 이메일/비밀번호 입력 필드
  - 로그인 버튼
  - 에러 메시지 표시
- **상태**:
  - Mock: 하드코딩된 관리자 계정 (PRD 명시)
  - 실제: Supabase Auth + RLS `is_admin()` 체크

#### 2.2.2 대시보드 (`/admin/dashboard`)
- **목적**: 시스템 전체 현황 및 통계
- **UI 요소**:
  - 통계 카드 (총 콘텐츠, 대기중, 분석중, 분석완료, 회원수) - 5개 카드
  - 실시간 활동 피드:
    - 최근 업로드된 대기중인 콘텐츠 (개수 제한: 최대 10개)
    - 최근 가입 요청 (개수 제한: 최대 10개)
- **데이터**:
  - Mock: 통계 데이터 객체, 대기중 콘텐츠 배열, 가입 요청 배열
  - 실제: Supabase 집계 쿼리

#### 2.2.3 회원 관리 리스트 (`/admin/users`)
- **목적**: 전체 회원 조회 및 관리
- **UI 요소**:
  - 회원 테이블 (이름, 이메일, 소속, 상태, 역할, 가입일)
  - 필터 (상태: 승인/대기/차단, 역할: member/admin)
  - 검색 (이메일, 이름, 소속)
  - 정렬 (가입일, 이름, 이메일)
  - 일괄 작업 버튼 (승인, 차단)
- **데이터**:
  - Mock: users 배열 (DATABASE_SCHEMA.md의 users 테이블 구조)
  - 실제: Supabase `public.users` JOIN `auth.users`

#### 2.2.4 회원 상세 (`/admin/users/[id]`)
- **목적**: 개별 회원 정보 조회 및 수정
- **UI 요소**:
  - 회원 정보 카드 (이름, 이메일, 소속, 가입일)
  - 권한 변경 버튼 (member ↔ admin)
  - 계정 활성화/비활성화 버튼
  - 활동 로그 테이블 (활동 시간, 활동 내용)
  - 업로드한 콘텐츠 목록
- **데이터**:
  - Mock: 단일 user 객체 + contents 배열
  - 실제: Supabase RLS 정책에 따른 조회/수정

#### 2.2.5 콘텐츠 관리 리스트 (`/admin/contents`)
- **목적**: 전체 콘텐츠 조회 및 관리
- **UI 요소**:
  - 콘텐츠 테이블 (썸네일, 파일명, 업로더, 컬렉션, 분석 상태, 발견 건수, 업로드일)
  - 필터 (업로더, 분석 상태, 컬렉션)
  - 검색 (파일명)
  - 정렬 (업로드일, 발견 건수)
  - AI 분석 실행 버튼 (개별/일괄)
  - 일괄 삭제 버튼
- **데이터**:
  - Mock: contents 배열 (file_path를 placeholder 이미지로)
  - 실제: Supabase `public.contents` JOIN `users`, `collections`

#### 2.2.6 콘텐츠 상세 (`/admin/contents/[id]`)
- **목적**: 개별 콘텐츠 상세 정보 및 분석 결과
- **UI 요소**:
  - 원본 이미지 표시
  - 메타데이터 (업로더, 컬렉션, 업로드일, 파일 크기)
  - AI 분석 상태 배지
  - 분석 결과 섹션:
    - LABEL_DETECTION 결과 (label_data JSONB)
    - TEXT_DETECTION 결과 (text_data JSONB)
  - 발견된 유사 콘텐츠 리스트
  - AI 분석 재실행 버튼
- **데이터**:
  - Mock: 단일 content 객체 + detected_contents 배열
  - 실제: Supabase 쿼리 + Google Vision API 결과

#### 2.2.7 비교 검토 리스트 (`/admin/review`)
- **목적**: 검토 대기 중인 발견 콘텐츠 목록
- **UI 요소**:
  - 검토 대기 테이블 (원본 썸네일, 발견 이미지, detection_type, 발견일)
  - 필터 (detection_type: full/partial/similar, 검토 상태)
  - 정렬 (발견일, detection_type)
  - 검토 버튼 → 비교 모달 열기
- **데이터**:
  - Mock: detected_contents 배열 (admin_review_status = 'pending')
  - 실제: Supabase `public.detected_contents` WHERE `admin_review_status = 'pending'`

#### 2.2.8 비교 검토 모달 (`/admin/review/[detectionId]`)
- **목적**: 원본과 발견 이미지 비교 및 판정
- **UI 요소**:
  - 좌우 분할 화면 (원본 | 발견 이미지)
  - 이미지 확대/축소 컨트롤
  - 이미지 동기화 스크롤
  - 메타데이터 표시 (source_url, page_title, detection_type)
  - 판정 버튼:
    - 일치 (match)
    - 불일치 (no_match)
    - 비교 불가 (cannot_compare)
  - 판정 사유 메모 입력
- **데이터**:
  - Mock: 단일 detected_content 객체 + 원본 content 객체
  - 실제: Supabase UPDATE `admin_review_status`, `reviewed_by`, `reviewed_at`

---

## 3. 컴포넌트 설계

### 3.1 레이아웃 컴포넌트

#### 3.1.1 AdminLayout
- **경로**: `components/admin/layout/AdminLayout.tsx`
- **책임**: 관리자 페이지 전체 레이아웃 제공
- **구조**:
  ```tsx
  <div className="flex h-screen">
    <AdminSidebar />
    <div className="flex-1 flex flex-col">
      <AdminHeader />
      <main className="flex-1 overflow-auto p-6">
        {children}
      </main>
    </div>
  </div>
  ```
- **Props**: `{ children: ReactNode }`

#### 3.1.2 AdminSidebar
- **경로**: `components/admin/layout/AdminSidebar.tsx`
- **책임**: 네비게이션 메뉴 제공
- **UI 요소**:
  - 로고
  - 메뉴 아이템 (대시보드, 회원 관리, 콘텐츠 관리, 비교 검토)
  - 활성 메뉴 하이라이트
- **상태**: `usePathname()`으로 활성 메뉴 판단

#### 3.1.3 AdminHeader
- **경로**: `components/admin/layout/AdminHeader.tsx`
- **책임**: 상단 헤더 (관리자 정보, 로그아웃)
- **UI 요소**:
  - 페이지 제목
  - 관리자 이름
  - 로그아웃 버튼

### 3.2 대시보드 컴포넌트

#### 3.2.1 StatsCard
- **경로**: `components/admin/dashboard/StatsCard.tsx`
- **책임**: 통계 수치 표시 카드
- **Props**:
  ```typescript
  {
    title: string;
    value: number | string;
    icon: ReactNode;
    change?: string;
    trend?: 'up' | 'down';
  }
  ```
- **UI**: Card 컴포넌트 (shadcn/ui)

#### 3.2.2 ActivityFeed
- **경로**: `components/admin/dashboard/ActivityFeed.tsx`
- **책임**: 실시간 활동 피드 (대기중 콘텐츠 + 가입 요청)
- **Props**:
  ```typescript
  {
    pendingContents: Array<{
      id: string;
      file_name: string;
      user_name: string;
      created_at: string;
    }>;
    pendingUsers: Array<{
      id: string;
      name: string;
      email: string;
      created_at: string;
    }>;
    maxItems?: number; // 각 섹션별 최대 표시 개수 (기본값: 10)
  }
  ```
- **UI**:
  - 두 개의 섹션으로 구분
  - "최근 대기중인 콘텐츠" 섹션
  - "최근 가입 요청" 섹션

### 3.3 회원 관리 컴포넌트

#### 3.3.1 UserTable
- **경로**: `components/admin/users/UserTable.tsx`
- **책임**: 회원 리스트 테이블
- **Props**:
  ```typescript
  {
    users: User[];
    onUserClick: (userId: string) => void;
    onBulkApprove?: (userIds: string[]) => void;
    onBulkBlock?: (userIds: string[]) => void;
  }
  ```
- **UI**: Table 컴포넌트 (shadcn/ui), Checkbox (일괄 선택)

#### 3.3.2 UserFilters
- **경로**: `components/admin/users/UserFilters.tsx`
- **책임**: 필터 및 검색 UI
- **Props**:
  ```typescript
  {
    onFilterChange: (filters: UserFilters) => void;
  }
  ```
- **상태**: Zustand Store에 필터 상태 저장

#### 3.3.3 UserDetailCard
- **경로**: `components/admin/users/UserDetailCard.tsx`
- **책임**: 회원 상세 정보 표시
- **Props**:
  ```typescript
  {
    user: User;
  }
  ```

#### 3.3.4 UserActionButtons
- **경로**: `components/admin/users/UserActionButtons.tsx`
- **책임**: 회원 관리 작업 버튼
- **Props**:
  ```typescript
  {
    user: User;
    onApprove: () => void;
    onBlock: () => void;
    onRoleChange: (role: 'member' | 'admin') => void;
  }
  ```

### 3.4 콘텐츠 관리 컴포넌트

#### 3.4.1 ContentTable
- **경로**: `components/admin/contents/ContentTable.tsx`
- **책임**: 콘텐츠 리스트 테이블
- **Props**:
  ```typescript
  {
    contents: Content[];
    onContentClick: (contentId: string) => void;
    onAnalyze?: (contentIds: string[]) => void;
    onDelete?: (contentIds: string[]) => void;
  }
  ```

#### 3.4.2 ContentFilters
- **경로**: `components/admin/contents/ContentFilters.tsx`
- **책임**: 필터 UI (업로더, 분석 상태, 컬렉션)

#### 3.4.3 AnalysisButton
- **경로**: `components/admin/contents/AnalysisButton.tsx`
- **책임**: AI 분석 실행 버튼 (로딩 상태 표시)
- **Props**:
  ```typescript
  {
    contentId: string;
    onAnalyze: (contentId: string) => void;
    isAnalyzing?: boolean;
  }
  ```

#### 3.4.4 AnalysisStatusBadge
- **경로**: `components/admin/contents/AnalysisStatusBadge.tsx`
- **책임**: 분석 상태 배지 (대기/진행/완료/실패)
- **Props**:
  ```typescript
  {
    status: boolean | null; // is_analyzed
    message?: string;
  }
  ```
- **색상**:
  - NULL: Gray (대기)
  - FALSE: Yellow (진행 중)
  - TRUE + no message: Green (완료)
  - FALSE + message: Red (실패)

### 3.5 비교 검토 컴포넌트

#### 3.5.1 ComparisonModal
- **경로**: `components/admin/review/ComparisonModal.tsx`
- **책임**: 비교 검토 모달 컨테이너
- **Props**:
  ```typescript
  {
    detectionId: string;
    isOpen: boolean;
    onClose: () => void;
    onReview: (status: ReviewStatus, memo?: string) => void;
  }
  ```
- **UI**: Dialog 컴포넌트 (shadcn/ui)

#### 3.5.2 ImageCompareViewer
- **경로**: `components/admin/review/ImageCompareViewer.tsx`
- **책임**: 좌우 이미지 비교 뷰어
- **기능**:
  - 확대/축소 (Zoom In/Out)
  - 동기화 스크롤
  - 전체 화면 토글
- **Props**:
  ```typescript
  {
    originalImageUrl: string;
    detectedImageUrl: string;
  }
  ```

#### 3.5.3 ReviewPanel
- **경로**: `components/admin/review/ReviewPanel.tsx`
- **책임**: 판정 패널 (일치/불일치/비교불가)
- **Props**:
  ```typescript
  {
    onReview: (status: ReviewStatus, memo?: string) => void;
  }
  ```
- **UI**: 라디오 버튼 + Textarea (메모)

#### 3.5.4 DetectionList
- **경로**: `components/admin/review/DetectionList.tsx`
- **책임**: 검토 대기 리스트
- **Props**:
  ```typescript
  {
    detections: DetectedContent[];
    onDetectionClick: (detectionId: string) => void;
  }
  ```

---

## 4. Mock 데이터 구조

### 4.1 TypeScript 타입 정의

```typescript
// lib/admin/types.ts

// DATABASE_SCHEMA.md의 users 테이블 기반
export interface User {
  id: string; // UUID
  name: string;
  email: string; // from auth.users
  organization: string | null;
  role: 'member' | 'admin';
  is_approved: boolean | null; // NULL: 대기, TRUE: 승인, FALSE: 거부
  created_at: string; // ISO 8601
  updated_at: string;
  // 추가 통계
  contents_count?: number;
  last_login?: string;
}

// DATABASE_SCHEMA.md의 collections 테이블 기반
export interface Collection {
  id: string;
  user_id: string;
  name: string;
  created_at: string;
  updated_at: string;
}

// DATABASE_SCHEMA.md의 contents 테이블 기반
export interface Content {
  id: string;
  user_id: string;
  collection_id: string | null;
  file_name: string;
  file_path: string; // Supabase Storage 경로
  is_analyzed: boolean | null; // NULL: 대기, FALSE: 진행, TRUE: 완료
  message: string | null;
  label_data: any | null; // JSONB
  text_data: any | null; // JSONB
  created_at: string;
  // JOIN 데이터
  user_name?: string;
  collection_name?: string;
  detected_count?: number;
}

// DATABASE_SCHEMA.md의 detected_contents 테이블 기반
export interface DetectedContent {
  id: string;
  content_id: string;
  source_url: string | null;
  image_url: string;
  page_title: string | null;
  detection_type: 'full' | 'partial' | 'similar';
  admin_review_status: 'pending' | 'match' | 'no_match' | 'cannot_compare';
  reviewed_by: string | null;
  reviewed_at: string | null;
  created_at: string;
  // JOIN 데이터
  original_image_path?: string;
  original_file_name?: string;
  reviewer_name?: string;
}

// 대시보드 통계
export interface DashboardStats {
  total_contents: number;           // 총 콘텐츠 수
  pending_contents: number;          // 대기중 (is_analyzed = NULL)
  analyzing_contents: number;        // 분석중 (is_analyzed = FALSE)
  completed_contents: number;        // 분석완료 (is_analyzed = TRUE)
  total_users: number;               // 총 회원 수
}

// 대기중인 콘텐츠 (활동 피드용)
export interface PendingContent {
  id: string;
  file_name: string;
  user_name: string;
  created_at: string;
}

// 가입 요청 (활동 피드용)
export interface PendingUser {
  id: string;
  name: string;
  email: string;
  created_at: string;
}

// 필터 상태
export interface UserFilters {
  status?: 'approved' | 'pending' | 'blocked' | null;
  role?: 'member' | 'admin' | null;
  search?: string;
}

export interface ContentFilters {
  user_id?: string;
  is_analyzed?: boolean | null;
  collection_id?: string;
  search?: string;
}

export interface ReviewFilters {
  detection_type?: 'full' | 'partial' | 'similar' | null;
  admin_review_status?: 'pending' | 'match' | 'no_match' | 'cannot_compare' | null;
}
```

### 4.2 Mock 데이터 생성

```typescript
// lib/admin/mock-data.ts

import { User, Content, DetectedContent, DashboardStats, PendingContent, PendingUser } from './types';

// 샘플 사용자 (PRD 임시 계정 포함)
export const mockUsers: User[] = [
  {
    id: 'admin-001',
    name: '관리자',
    email: 'pulwind00@gmail.com',
    organization: 'UTRBOX',
    role: 'admin',
    is_approved: true,
    created_at: '2025-10-01T00:00:00Z',
    updated_at: '2025-10-24T00:00:00Z',
    contents_count: 0,
    last_login: '2025-10-24T10:00:00Z',
  },
  {
    id: 'user-001',
    name: '김철수',
    email: 'kim@example.com',
    organization: '에이전시A',
    role: 'member',
    is_approved: true,
    created_at: '2025-10-10T00:00:00Z',
    updated_at: '2025-10-24T00:00:00Z',
    contents_count: 15,
    last_login: '2025-10-23T14:00:00Z',
  },
  {
    id: 'user-002',
    name: '이영희',
    email: 'lee@example.com',
    organization: '스튜디오B',
    role: 'member',
    is_approved: null, // 승인 대기
    created_at: '2025-10-23T00:00:00Z',
    updated_at: '2025-10-23T00:00:00Z',
    contents_count: 0,
  },
  // ... 추가 샘플
];

// 샘플 콘텐츠
export const mockContents: Content[] = [
  {
    id: 'content-001',
    user_id: 'user-001',
    collection_id: 'collection-001',
    file_name: 'image01.jpg',
    file_path: 'user-001/1729680000_image01.jpg',
    is_analyzed: true,
    message: null,
    label_data: {
      labels: [
        { description: 'Nature', score: 0.95 },
        { description: 'Landscape', score: 0.89 },
      ],
    },
    text_data: {
      text: 'Sample detected text',
    },
    created_at: '2025-10-20T10:00:00Z',
    user_name: '김철수',
    collection_name: '풍경 사진',
    detected_count: 3,
  },
  {
    id: 'content-002',
    user_id: 'user-001',
    collection_id: null,
    file_name: 'logo.png',
    file_path: 'user-001/1729680100_logo.png',
    is_analyzed: null, // 분석 대기
    message: null,
    label_data: null,
    text_data: null,
    created_at: '2025-10-24T09:00:00Z',
    user_name: '김철수',
    collection_name: null,
    detected_count: 0,
  },
  // ... 추가 샘플
];

// 샘플 발견 콘텐츠
export const mockDetectedContents: DetectedContent[] = [
  {
    id: 'detection-001',
    content_id: 'content-001',
    source_url: 'https://example.com/page1',
    image_url: 'https://example.com/images/similar1.jpg',
    page_title: 'Example Page 1',
    detection_type: 'full',
    admin_review_status: 'pending',
    reviewed_by: null,
    reviewed_at: null,
    created_at: '2025-10-20T12:00:00Z',
    original_image_path: 'user-001/1729680000_image01.jpg',
    original_file_name: 'image01.jpg',
  },
  {
    id: 'detection-002',
    content_id: 'content-001',
    source_url: null,
    image_url: 'https://example.com/images/similar2.jpg',
    page_title: null,
    detection_type: 'partial',
    admin_review_status: 'match',
    reviewed_by: 'admin-001',
    reviewed_at: '2025-10-21T10:00:00Z',
    created_at: '2025-10-20T12:00:00Z',
    original_image_path: 'user-001/1729680000_image01.jpg',
    original_file_name: 'image01.jpg',
    reviewer_name: '관리자',
  },
  // ... 추가 샘플
];

// 대시보드 통계
export const mockDashboardStats: DashboardStats = {
  total_contents: 42,        // 총 콘텐츠
  pending_contents: 8,        // 대기중 (is_analyzed = NULL)
  analyzing_contents: 5,      // 분석중 (is_analyzed = FALSE)
  completed_contents: 29,     // 분석완료 (is_analyzed = TRUE)
  total_users: 18,            // 총 회원수
};

// 대기중인 콘텐츠 (최근 10개)
export const mockPendingContents: PendingContent[] = [
  {
    id: 'content-002',
    file_name: 'logo.png',
    user_name: '김철수',
    created_at: '2025-10-24T09:00:00Z',
  },
  {
    id: 'content-010',
    file_name: 'banner.jpg',
    user_name: '이영희',
    created_at: '2025-10-24T08:45:00Z',
  },
  // ... 추가 샘플 (최대 10개)
];

// 가입 요청 (최근 10개)
export const mockPendingUsers: PendingUser[] = [
  {
    id: 'user-002',
    name: '이영희',
    email: 'lee@example.com',
    created_at: '2025-10-23T00:00:00Z',
  },
  {
    id: 'user-015',
    name: '박민수',
    email: 'park@example.com',
    created_at: '2025-10-22T14:30:00Z',
  },
  // ... 추가 샘플 (최대 10개)
];

// Placeholder 이미지 URL (실제 Supabase Storage 대신)
export const getPlaceholderImageUrl = (width: number = 400, height: number = 300, seed?: string): string => {
  return `https://picsum.photos/seed/${seed || 'random'}/${width}/${height}`;
};
```

### 4.3 Zustand Store (Admin 상태 관리)

```typescript
// lib/admin/store.ts

import { create } from 'zustand';
import { UserFilters, ContentFilters, ReviewFilters } from './types';

interface AdminState {
  // 필터 상태
  userFilters: UserFilters;
  contentFilters: ContentFilters;
  reviewFilters: ReviewFilters;

  // 선택 상태
  selectedUserIds: string[];
  selectedContentIds: string[];

  // 액션
  setUserFilters: (filters: UserFilters) => void;
  setContentFilters: (filters: ContentFilters) => void;
  setReviewFilters: (filters: ReviewFilters) => void;
  toggleUserSelection: (userId: string) => void;
  toggleContentSelection: (contentId: string) => void;
  clearSelections: () => void;
}

export const useAdminStore = create<AdminState>((set) => ({
  // 초기 상태
  userFilters: {},
  contentFilters: {},
  reviewFilters: {},
  selectedUserIds: [],
  selectedContentIds: [],

  // 액션
  setUserFilters: (filters) => set({ userFilters: filters }),
  setContentFilters: (filters) => set({ contentFilters: filters }),
  setReviewFilters: (filters) => set({ reviewFilters: filters }),
  toggleUserSelection: (userId) =>
    set((state) => ({
      selectedUserIds: state.selectedUserIds.includes(userId)
        ? state.selectedUserIds.filter((id) => id !== userId)
        : [...state.selectedUserIds, userId],
    })),
  toggleContentSelection: (contentId) =>
    set((state) => ({
      selectedContentIds: state.selectedContentIds.includes(contentId)
        ? state.selectedContentIds.filter((id) => id !== contentId)
        : [...state.selectedContentIds, contentId],
    })),
  clearSelections: () => set({ selectedUserIds: [], selectedContentIds: [] }),
}));
```

---

## 5. 작업 단계 및 일정

### 5.1 Phase 1: 기반 구조 (1-2일)

#### 작업 1.1: 관리자 레이아웃 구축
- [ ] `app/admin/layout.tsx` 생성 (AdminLayout 적용)
- [ ] `components/admin/layout/AdminLayout.tsx` 구현
- [ ] `components/admin/layout/AdminSidebar.tsx` 구현 (네비게이션 메뉴)
- [ ] `components/admin/layout/AdminHeader.tsx` 구현 (관리자 정보, 로그아웃)
- [ ] 라우팅 설정 (`/admin/*`)

**산출물**:
- 관리자 전용 레이아웃
- 네비게이션 메뉴 (대시보드, 회원, 콘텐츠, 검토)

#### 작업 1.2: Mock 데이터 및 타입 정의
- [ ] `lib/admin/types.ts` 생성 (TypeScript 타입 정의)
- [ ] `lib/admin/mock-data.ts` 생성 (샘플 데이터)
- [ ] `lib/admin/store.ts` 생성 (Zustand Store)
- [ ] Placeholder 이미지 헬퍼 함수

**산출물**:
- 타입 정의 파일
- Mock 데이터 파일
- 상태 관리 Store

### 5.2 Phase 2: 핵심 페이지 (2-3일)

#### 작업 2.1: 대시보드 페이지
- [ ] `app/admin/dashboard/page.tsx` 생성
- [ ] `components/admin/dashboard/StatsCard.tsx` 구현
- [ ] `components/admin/dashboard/ActivityFeed.tsx` 구현 (대기중 콘텐츠 + 가입 요청)
- [ ] Mock 통계 데이터 연동 (5개 통계 카드)
- [ ] Mock 대기 데이터 연동 (mockPendingContents, mockPendingUsers)

**산출물**:
- 대시보드 페이지 (5개 통계 카드, 활동 피드)

#### 작업 2.2: 회원 관리 리스트
- [ ] `app/admin/users/page.tsx` 생성
- [ ] `components/admin/users/UserTable.tsx` 구현 (shadcn/ui Table)
- [ ] `components/admin/users/UserFilters.tsx` 구현
- [ ] 필터/검색 로직 연동 (Zustand Store)
- [ ] 정렬 기능 구현

**산출물**:
- 회원 리스트 페이지 (테이블, 필터, 검색)

#### 작업 2.3: 회원 상세 페이지
- [ ] `app/admin/users/[id]/page.tsx` 생성
- [ ] `components/admin/users/UserDetailCard.tsx` 구현
- [ ] `components/admin/users/UserActionButtons.tsx` 구현
- [ ] 활동 로그 테이블 구현
- [ ] 업로드 콘텐츠 목록 구현

**산출물**:
- 회원 상세 페이지 (정보 카드, 작업 버튼, 활동 로그)

### 5.3 Phase 3: 콘텐츠 관리 (2-3일)

#### 작업 3.1: 콘텐츠 리스트 페이지
- [ ] `app/admin/contents/page.tsx` 생성
- [ ] `components/admin/contents/ContentTable.tsx` 구현
- [ ] `components/admin/contents/ContentFilters.tsx` 구현
- [ ] `components/admin/contents/AnalysisStatusBadge.tsx` 구현
- [ ] `components/admin/contents/AnalysisButton.tsx` 구현
- [ ] 필터/검색 로직 연동

**산출물**:
- 콘텐츠 리스트 페이지 (테이블, 필터, 분석 버튼)

#### 작업 3.2: 콘텐츠 상세 페이지
- [ ] `app/admin/contents/[id]/page.tsx` 생성
- [ ] 원본 이미지 표시 컴포넌트
- [ ] 메타데이터 섹션 구현
- [ ] AI 분석 결과 섹션 구현 (label_data, text_data JSONB 파싱)
- [ ] 발견 콘텐츠 리스트 구현

**산출물**:
- 콘텐츠 상세 페이지 (이미지, 메타, 분석 결과, 발견 리스트)

### 5.4 Phase 4: 비교 검토 (2-3일)

#### 작업 4.1: 검토 대기 리스트
- [ ] `app/admin/review/page.tsx` 생성
- [ ] `components/admin/review/DetectionList.tsx` 구현
- [ ] 필터/정렬 기능 구현
- [ ] 검토 버튼 → 모달 열기 로직

**산출물**:
- 검토 대기 리스트 페이지

#### 작업 4.2: 비교 검토 모달
- [ ] `app/admin/review/[detectionId]/page.tsx` 또는 모달 컴포넌트
- [ ] `components/admin/review/ComparisonModal.tsx` 구현 (Dialog)
- [ ] `components/admin/review/ImageCompareViewer.tsx` 구현
  - 확대/축소 기능
  - 동기화 스크롤
- [ ] `components/admin/review/ReviewPanel.tsx` 구현
  - 라디오 버튼 (일치/불일치/비교불가)
  - 메모 입력
- [ ] 판정 결과 저장 로직 (Mock)

**산출물**:
- 비교 검토 모달 (좌우 이미지, 판정 패널)

### 5.5 Phase 5: 마무리 (1일)

#### 작업 5.1: 관리자 로그인 페이지
- [ ] `app/admin/login/page.tsx` 생성
- [ ] 로그인 폼 구현 (이메일/비밀번호)
- [ ] Mock 인증 로직 (하드코딩)
- [ ] 에러 메시지 표시

**산출물**:
- 관리자 로그인 페이지

#### 작업 5.2: 통합 테스트 및 반응형 조정
- [ ] 모든 페이지 네비게이션 테스트
- [ ] 반응형 디자인 확인 (Desktop 1920px, Tablet 768px)
- [ ] 필터/검색 기능 동작 확인
- [ ] 모달 열기/닫기 테스트
- [ ] 한국어 폰트 확인

**산출물**:
- 완성된 관리자 페이지 퍼블리싱

### 5.6 예상 일정

| Phase | 작업 내용 | 예상 기간 |
|-------|----------|----------|
| **Phase 1** | 기반 구조 | 1-2일 |
| **Phase 2** | 핵심 페이지 | 2-3일 |
| **Phase 3** | 콘텐츠 관리 | 2-3일 |
| **Phase 4** | 비교 검토 | 2-3일 |
| **Phase 5** | 마무리 | 1일 |
| **총 기간** | | **8-12일** |

---

## 6. 파일 구조

```
project/
├── app/
│   └── admin/
│       ├── layout.tsx                      # AdminLayout 적용
│       ├── login/
│       │   └── page.tsx                   # 관리자 로그인
│       ├── dashboard/
│       │   └── page.tsx                   # 대시보드
│       ├── users/
│       │   ├── page.tsx                   # 회원 리스트
│       │   └── [id]/
│       │       └── page.tsx               # 회원 상세
│       ├── contents/
│       │   ├── page.tsx                   # 콘텐츠 리스트
│       │   └── [id]/
│       │       └── page.tsx               # 콘텐츠 상세
│       └── review/
│           ├── page.tsx                   # 검토 대기 리스트
│           └── [detectionId]/
│               └── page.tsx               # 비교 검토 (또는 모달)
│
├── components/
│   └── admin/
│       ├── layout/
│       │   ├── AdminLayout.tsx            # 관리자 레이아웃
│       │   ├── AdminSidebar.tsx           # 사이드바 네비게이션
│       │   └── AdminHeader.tsx            # 헤더
│       ├── dashboard/
│       │   ├── StatsCard.tsx              # 통계 카드
│       │   └── ActivityFeed.tsx           # 활동 피드
│       ├── users/
│       │   ├── UserTable.tsx              # 회원 테이블
│       │   ├── UserFilters.tsx            # 필터
│       │   ├── UserDetailCard.tsx         # 상세 카드
│       │   └── UserActionButtons.tsx      # 작업 버튼
│       ├── contents/
│       │   ├── ContentTable.tsx           # 콘텐츠 테이블
│       │   ├── ContentFilters.tsx         # 필터
│       │   ├── AnalysisButton.tsx         # 분석 버튼
│       │   └── AnalysisStatusBadge.tsx    # 상태 배지
│       └── review/
│           ├── ComparisonModal.tsx        # 비교 모달
│           ├── ImageCompareViewer.tsx     # 이미지 뷰어
│           ├── ReviewPanel.tsx            # 판정 패널
│           └── DetectionList.tsx          # 검토 리스트
│
├── lib/
│   └── admin/
│       ├── types.ts                       # TypeScript 타입 정의
│       ├── mock-data.ts                   # Mock 데이터
│       └── store.ts                       # Zustand Store
│
└── docs/
    └── 004_관리자_페이지_퍼블리싱_계획.md  # 이 문서
```

---

## 7. 디자인 시스템

### 7.1 Color Palette (PRD 6.1 준수)

```typescript
// Tailwind CSS 클래스 기준
const colors = {
  primary: 'bg-blue-500',      // #3B82F6
  secondary: 'bg-gray-600',    // #6B7280
  success: 'bg-green-500',     // #10B981
  warning: 'bg-yellow-500',    // #F59E0B
  error: 'bg-red-500',         // #EF4444
};
```

### 7.2 Typography

- **Font Family**:
  - Latin: `Inter` (Google Fonts)
  - Korean: `Noto Sans KR` (Google Fonts)
- **Font Sizes**:
  - Headings: `text-3xl` (32px), `text-2xl` (24px), `text-xl` (20px), `text-lg` (16px)
  - Body: `text-sm` (14px), `text-xs` (12px)

### 7.3 Spacing (4px 그리드)

```typescript
// Tailwind CSS spacing
const spacing = {
  xs: 'p-1',    // 4px
  sm: 'p-2',    // 8px
  md: 'p-4',    // 16px
  lg: 'p-6',    // 24px
  xl: 'p-8',    // 32px
};
```

### 7.4 상태별 색상 코딩

#### 회원 승인 상태
- **승인됨** (is_approved = TRUE): `badge-success` (Green)
- **대기 중** (is_approved = NULL): `badge-warning` (Yellow)
- **거부됨** (is_approved = FALSE): `badge-error` (Red)

#### 분석 상태
- **대기** (is_analyzed = NULL): `badge-secondary` (Gray)
- **진행 중** (is_analyzed = FALSE): `badge-warning` (Yellow)
- **완료** (is_analyzed = TRUE, no message): `badge-success` (Green)
- **실패** (is_analyzed = FALSE, with message): `badge-error` (Red)

#### 검토 상태
- **대기** (admin_review_status = 'pending'): `badge-warning` (Yellow)
- **일치** (admin_review_status = 'match'): `badge-error` (Red)
- **불일치** (admin_review_status = 'no_match'): `badge-success` (Green)
- **비교 불가** (admin_review_status = 'cannot_compare'): `badge-secondary` (Gray)

#### Detection Type
- **완전 일치** (detection_type = 'full'): `badge-error` (Red)
- **부분 일치** (detection_type = 'partial'): `badge-warning` (Yellow)
- **시각적 유사** (detection_type = 'similar'): `badge-secondary` (Gray)

### 7.5 shadcn/ui 컴포넌트 활용

```bash
# 필요한 shadcn/ui 컴포넌트 설치
npx shadcn-ui@latest add table
npx shadcn-ui@latest add card
npx shadcn-ui@latest add badge
npx shadcn-ui@latest add button
npx shadcn-ui@latest add dialog
npx shadcn-ui@latest add input
npx shadcn-ui@latest add select
npx shadcn-ui@latest add checkbox
npx shadcn-ui@latest add radio-group
npx shadcn-ui@latest add textarea
npx shadcn-ui@latest add avatar
```

---

## 8. 기술 스택

### 8.1 핵심 기술 (PRD 2.1 준수)

- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript 5.x
- **Styling**: Tailwind CSS 3.x
- **State Management**: Zustand
- **UI Components**: shadcn/ui
- **Form Handling**: React Hook Form + Zod (필요 시)

### 8.2 개발 도구

- **Linting**: ESLint
- **Formatting**: Prettier
- **Type Checking**: TypeScript Compiler

### 8.3 외부 라이브러리 (최소화)

- **date-fns**: 날짜 포맷팅
- **clsx**: 조건부 클래스 네이밍
- **lucide-react**: 아이콘

---

## 9. 주의사항 및 제약사항

### 9.1 PRD 범위 준수

- ✅ **구현할 것**: PRD 3.2에 명시된 관리자 포털 기능만 구현
- ❌ **구현하지 말 것**:
  - 명시되지 않은 추가 기능 (예: 통계 내보내기, 이메일 알림)
  - 실제 API 연동 (Mock 데이터만 사용)
  - 인증 로직 (Mock 인증만)

### 9.2 데이터베이스 스키마 준수

- **DATABASE_SCHEMA.md 엄격히 따름**
- 컬럼명, 타입, 제약조건 정확히 일치
- RLS 정책 구조 이해 (향후 API 연동 대비)

### 9.3 Mock 데이터 단계

- 현재 단계는 **퍼블리싱 작업**
- 실제 Supabase 연동은 **별도 작업**
- Mock 데이터는 `lib/admin/mock-data.ts`에서 중앙 관리

### 9.4 접근성 (Accessibility)

- ARIA 레이블 추가
- 키보드 네비게이션 지원
- 충분한 색상 대비 (WCAG AA 기준)

### 9.5 성능 최적화

- **테이블 가상화**: 대용량 데이터 렌더링 시 `react-virtual` 고려
- **이미지 최적화**: Next.js Image 컴포넌트 사용
- **코드 스플리팅**: 동적 import 활용

### 9.6 반응형 디자인

- **Desktop 우선**: 1920px 기준 레이아웃
- **Tablet 지원**: 768px 이상
- **Mobile**: 필수는 아니지만, 기본 레이아웃 깨지지 않도록

### 9.7 브라우저 지원

- Chrome, Firefox, Safari, Edge (최신 버전)

### 9.8 테스트 체크리스트

#### 기능 테스트
- [ ] 모든 페이지 네비게이션 동작
- [ ] 필터/검색 기능 동작
- [ ] 정렬 기능 동작
- [ ] 모달 열기/닫기
- [ ] 테이블 페이지네이션
- [ ] 일괄 선택/해제

#### 디자인 테스트
- [ ] 반응형 디자인 (Desktop 1920px, Tablet 768px)
- [ ] 색상 스킴 정확성
- [ ] 한국어 폰트 (Noto Sans KR) 적용
- [ ] 상태 배지 색상 정확성
- [ ] 일관된 spacing, radius, shadow

#### 접근성 테스트
- [ ] 키보드 네비게이션
- [ ] ARIA 레이블
- [ ] 색상 대비

### 9.9 제약사항

- **API 연동 제외**: 현재 단계에서는 Mock 데이터만 사용
- **인증 로직 제외**: 하드코딩된 관리자 계정만 사용
- **Google Vision API 제외**: 분석 결과는 Mock JSONB 데이터
- **Realtime 기능 제외**: Supabase Realtime 연동은 향후 작업

### 9.10 향후 작업 (이 문서 범위 밖)

1. Supabase API 연동
2. 실제 인증 로직 (Supabase Auth + RLS)
3. Google Vision API 통합
4. Supabase Storage 연동
5. Realtime 업데이트
6. 이메일 알림
7. 통계 내보내기 (CSV, PDF)

---

## 부록

### A. 참고 문서

- [PRD.md](../PRD.md): 프로젝트 요구사항 정의
- [DATABASE_SCHEMA.md](../DATABASE_SCHEMA.md): 데이터베이스 스키마
- [FOLDER_STRUCTURE.md](../FOLDER_STRUCTURE.md): 프로젝트 폴더 구조

### B. 외부 리소스

- [Next.js 14 Documentation](https://nextjs.org/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [shadcn/ui Components](https://ui.shadcn.com/)
- [Zustand Documentation](https://zustand-demo.pmnd.rs/)

### C. 샘플 코드 스니펫

#### Supabase 쿼리 예시 (향후 API 연동 시 참고)

```typescript
// 회원 리스트 조회 (관리자만 가능)
const { data: users, error } = await supabase
  .from('users')
  .select(`
    *,
    auth_users:auth.users!inner(email)
  `)
  .order('created_at', { ascending: false });

// 콘텐츠 리스트 조회 (발견 건수 포함)
const { data: contents, error } = await supabase
  .from('contents')
  .select(`
    *,
    users(name),
    collections(name),
    detected_contents(count)
  `)
  .order('created_at', { ascending: false });

// 검토 대기 리스트 조회
const { data: detections, error } = await supabase
  .from('detected_contents')
  .select(`
    *,
    contents(file_path, file_name)
  `)
  .eq('admin_review_status', 'pending')
  .order('created_at', { ascending: false });

// 판정 결과 저장
const { error } = await supabase
  .from('detected_contents')
  .update({
    admin_review_status: 'match',
    reviewed_by: userId,
    reviewed_at: new Date().toISOString(),
  })
  .eq('id', detectionId);
```

---

**문서 작성 완료**
**다음 단계**: Phase 1 작업 시작 (관리자 레이아웃 구축)
